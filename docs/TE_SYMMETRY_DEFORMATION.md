# TE対称曲線Projectの非アフィン変形

2026-09-15 JST、開始HEAD `2a2b9c7`。D02の非アフィン対称Project変形基盤を対象とする。
受入条件は同じ対称セクターと面の保持、固定したメッシュ番号/分割履歴、全段階の幾何検査、
直接/鏡映の実TE FEM、独立体積積分とMaxwell尺度則、CLIの完全なProject一致である。

`deform_curved_project`の一律拒否を、TEでは同一端条件・高々一つの対称端を要求する検査へ置換した。
変形後のCaseもTE条件と元セクターに照合する。曲線数・順序付きタグ・弦分割数は変えられない。
TEは固定RFメタデータを要求し、加速区間を新設しない。
TMの対称/鏡映拒否、非組立・native P2・凍結marked条件は維持する。

境界変位のLaplace延長は元の弦メッシュで解く既存方式であり、電磁場の代用ではない。
各履歴接頭部分について、接続/境界番号/曲線分率、正のP2写像・辺・角度と予算を確認する。
TEで鏡映指定がある場合は各段階の全領域幾何も構成・検査する。
元半領域のProjectとして変形し、`reflect_full`を保存する。
非アフィンtune/Studyの外側のガードはまだ変更していない。専用GUI受入も後続である。

## 検証

記録は`out/te-symmetry-deformation-20260915/`。
新3テストは旧ガードで9 errors/1 failure、0.545秒となることを再現し、実装後5.578秒でPASS。
両端位置・電気/磁気対称・直接/鏡映の8組で、marked→uniformの凍結履歴と境界面を保持する。
元Project不変、メッシュ番号/曲線分率一致、RF方針・タグ変更・傾いた対称面・TM半領域の拒否を確認する。

実楕円弧の中心/軸長を異なる割合で変え、全節点を単一アフィン写像では移せないことを検査する。
得られたProjectを実TE FEMで解き、鏡映全領域のEφ追跡がPASSすることを確認する。
倍寸の周波数、全係数、U/G/Q0/壁損失を分離し、Maxwell尺度則を1e-10で検査する。
これらは合成形状であり、実測空洞ではない。相似則から離散化精度や連続した物理枝を保証しない。

専用nativeは変形後と倍寸の2半領域固有値計算を実行し、既存鏡映元解1件を再利用した。
全領域の保存比較・再生がPASS、重なり約0.998883。
独立Green境界積分と比較写像の体積を照合した。最初は符号付き積分を正体積と直接比較して失敗した。
この保存境界は当該Green積分に対して時計回りなので、負号を対応させて正体積であることを要求した。
絶対値で任意の向きを隠しておらず、製品の幾何・許容差は変えていない。
完了済み2ジョブのProject完全一致を検査して再利用し、失敗後に固有値計算を繰り返していない。

全体積は約0.004523251286/0.005510575001 m³で両方法が1e-12以内で一致。
周波数/係数/RFの尺度差最大1.891e-15、元30 nativeファイルのhash保持を確認した。
CLIの`deform-curved-project`出力は、実計算したAPI Projectと全項目が一致し、鏡映指定も保持した。追加FEM0。
新規外部資料・旧コード・旧バイナリは使用していない。

関連`test_curved_harmonic_deformation test_curved_harmonic_study test_te_curved_tuning`は19件467.930秒PASS。
既存の独立cotangent変位エネルギー、Green形状量、折れ返り拒否、固定履歴、実Study/調整・保存再開を含む。
full/seed validator、hosted CI、実GUIは今回実行していない。
[集約結果](../benchmarks/tuning/te-symmetry-deformation-20260915.json)を保存した。全handle終端。
次は非アフィン対称tune/Studyの入力・比較・再開経路を接続する。全計画goalは継続中。
