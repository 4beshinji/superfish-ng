# S05: 平面磁場の力・トルク報告保存・再検証・CLI

2026-09-13 JST。後続限定課題。未実装・未受入。

最初の保存範囲は成功した平面線形スカラーnative。厳密なrequestで対象region IDs、元頂点のP1重み、トルク原点、仮想仕事の並進/回転刻みを指定する。仮想仕事を省く場合は明示nullとし、応力結果のみで精度検証済みとは扱わない。元5ファイルのSHA256、元Case/FEM/重み・真空遷移条件、力、重み付きトルクと節点回転に対応する応力トルク、両者の差と求積診断を報告へ保持する。

指定された仮想仕事は実FEMで再求解し、全±変位Case、固定された境界・源電流、停留ポテンシャルとエネルギー/源仕事、元係数のhash、差分刻みと単位を保存する。節点回転の差分は同じ節点速度の応力トルクと比較し、二次のスカラー重み回転と暗黙に同一視しない。元場の補正、解析力の代入、厳密な連続場誤差上界は行わない。

保存はnative外の完了JSONで、上書き不可。明示出典を読んで同じFEM/応力/仮想仕事を再計算し、全JSONを照合する。requestの変更、出典差/未知形式/失敗native、物理・単位・元重み/元場・変位Case/停留ポテンシャルの改変、途中変更、リンクと出版中断を拒否する。CLIはanalyze-planar-magnetic-force RUN --request REQUEST --out REPORT、replay-planar-magnetic-force RUN REPORT。成功0、不正入力/保存/改変2。capabilitiesは専用範囲を明示する。

受入は単電流の非零力・電流対の非零トルク、P1/P2の二つの応力トルク、線形透磁率を持つ対象、原点/尺度/電流反転、仮想仕事の指定/null、API/CLIの全JSONとバイト一致、出典ファイル不変と全改変拒否。標準検証と既存周波数/RF差を確認して限定受入する。GUI、B-H/反跳・軸対称の力/トルクは後続工程。
