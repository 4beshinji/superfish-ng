# G03: 同じ支持円上の有限オフセット弧の分類

2026-09-13 UTC。[最終受入結果](G03_FINITE_OFFSETS_CHECKPOINT.md)により主915sourceへ統合・限定受入済み。現在の新規診断出力は版4、要求は版1。保存診断版1/2/3は各当時の分類規則で全内容を再検証する。以下の版2/3・候補検証中・未完の記録は開発段階の履歴であり、先行候補の個別標準をPASSとは扱わない。最終909sourceの標準1456件・主41unit・独立比較・実Chrome初回/再起動が受入証拠である。

2026-09-13。候補実装・検証中、主未統合・未受入。[G03の弧端・退化残件](G03_ACCEPTANCE.md)から選んだ限定拡張。[既存診断](OFFSET_DEGENERACIES.md)の事実証明と有限領域全体の分類を分ける契約を維持する。

開始時の解析的な4例を `out/coincident-circle-arcs-development-20260913/initial-invariant.json` に記録した。同一パラメータの区間重なり、共有端点、離れた有限弧、反対向きの支持回転では、集合の交わりを解析的に判定できるが、現行診断は有限領域の完全性を証明しない。誤った合格を出していたという記録ではない。

対象は、既存の厳密有理数モデルで中心と非零支持半径が一致し、符号付き半径を含む二つの回転の相対関係が厳密な四分の一回転で表せる円弧。元の有理数パラメータ区間を保ち、相対位相と整数周期を加えた有限区間の交わりを全て検査する。共通の一般回転でも、相対関係が厳密に一致すれば扱える。二進回転のノルムを1へ丸めず、既存モデルで証明できない支持円は対象へ加えない。

位相の周期には真のπの有理数区間を使う。`math.pi` を厳密な周期として使わず、既存の離散的な正負置換回転の規約と、格納された開始角/掃引角の厳密な二進値を区別する。区間端の等号は有理数として一致するときだけ認め、近傍への吸着はしない。πの囲い込みは逆正接の交代級数と加法関係から導出し、項数/幅の予算不足を未確認として保持する。

受入条件は次のとおり。

- 離隔、共有端点、正の長さの重なり、周期をまたぐ二つの重なり成分を、全候補周期の証拠付きで分類する。有限中心点数と無限パラメータ対を区別する。
- 弧の交換、逆向きと距離符号の反転、尺度/平行移動、負のオフセット半径、指定部分区間で同じ集合の判定を保つ。1 ULPの端点差を等号へ丸めない。
- 独立な解析区間と別方式の高精度数値参照で確認する。未確認な相対回転、潰れ、一般楕円/双曲線の既存判定と予算不足を保持する。
- strict入力、CLI保存/再実行、構築診断の保存/再読込を確認する。診断の追加だけでフィレット候補を選択したり、構築を適用可能へ変更したりしない。
- 専用unittest、標準検証、既存の周波数とRF量の回帰、主ツリー統合時の検証を通す。数値許容差や既存ベンチマークを変更しない。

一般の相対回転、楕円/双曲線の重解・自己交差、退化からの候補構築、旧曲線指定と対象版、物理ピーク収束は引き続き別の残件。O02の現在の受入を確定させてから、この拡張の主統合へ進む。新しい外部資料・依存・旧版資産は使用しない。

候補は `/tmp/superfish-coincident-circle-arcs-20260913`。開始時4例を分類でき、新規7件と既存診断/構築14件の計21unitが合格。旧保存診断3例の再読込が単純な分類変更では失敗することを先に記録し、新しい診断出力を版2、旧構築診断版1の再検証を従来規則へ固定する方式で解消した。構築文書自体と適用可否は変更しない。元版1のfixtureはこの自前実装から生成した保存物で、旧SUPERFISHの資産ではない。

独立比較の初回は、Decimal参照が同じ位相を持つ厳密端点の相殺を有限桁で評価し、精度未確認として停止した。参照側では有理数部分とπの係数を分け、同じ位相の等号を厳密に比較する。初回driverとログを保持し、診断本体や許容差は変更していない。修正後2688条件はPASS。[候補と証拠](COINCIDENT_CIRCLE_ARCS.md)を参照。標準検証、実ブラウザーと主統合の受入は後続。
