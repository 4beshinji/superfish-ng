# 次の実装タスク

各タスクの受入条件を満たしてから次へ進む。課題番号はローカル用であり、外部Issueは作成していない。

2026-09-05: P0-00完了。26テスト、例題、収束検証、配布ZIPのmanifest検査と展開後の実行が合格。
記録は [LOCAL_DEVELOPMENT.md](LOCAL_DEVELOPMENT.md)。次の数値開発課題はP0-01。

2026-09-05追記: ユーザー指定の優先マイルストーンは
[セミナー4資料の例題計算・可視化](MILESTONE_SEMINAR.md)。直近はS1〜S6の順で進める。
Wine版SUPERFISHの3形状照合は完了（[記録](SUPERFISH_COMPARISON.md)）。
これはP0-01のNGSolve/GetDP照合を実行したという意味ではない。

S1進捗: 全領域Pillbox TM010/TM011、長さ掃引、場の図・CSV・結果HTML、符号付きSF7照合を実装済み。
半領域の電気／磁気対称境界・全空洞鏡映も実装・検証済み。次はS2の垂直段差。[検証結果](SEMINAR_PILLBOX.md)を参照。
S2進捗: 適合垂直段差メッシュと4セルflatの計算・初回Wine照合を実装。次は両コードの収束・同定ゲートと円弧。[記録](SEMINAR_MULTICELL.md)。
最新進捗: flat4/rounded4の全モードWine照合・収束が合格。交差分割により7セルの微小R/Q収束も合格。
端部full-cell比較の数値・画面検査も合格。次は7セルWine照合とS6一括検証。
rounded7のWine追加細分は実行ハンドルと出力の状態を確認して継続し、重複起動しない。

| ID | 依存 | 内容 | 受入条件 |
|---|---|---|---|
| P0-00 | seed | 既存作業フォルダをPJルート化し、開発環境と配布境界を整備 | seed checksum一致、既存23テスト・例題・収束検証が合格。配布ZIPが既存資産・リンク先を取り込まず、展開後も実行可能 |
| P0-01 | M0 | NGSolve/GetDPによる独立照合 | 同一2ケース、独立メッシュ収束、周波数・R/Q・Gの比較JSON。未実行は明記 |
| P0-02 | P0-01 | 境界タグ付き非構造三角形の読み込み | 軸/PECを保持し、単位・座標順を明示。不正トポロジー拒否とpillbox合格 |
| P0-03 | P0-02 | 任意メッシュからの軸線抽出・線積分 | 構造格子のnode strideへの依存をなくし、Ez軸上積分と壁損失を維持 |
| P0-04 | P0-01 | cavsim2dとの役割比較ADR | 依存物監査、導入手順、同形状の誤差対DOF、採用/継続の理由 |
| P1-01 | P0-02 | P2 u要素または高次バックエンド | 周波数と軸上Ez/RQの収束改善を個別に証明 |
| P1-02 | P1-01 | 円弧/楕円＋接線で滑らかなセル形状 | 自己交差・微小隙間なし、曲率と最小丸め半径を入力として保持 |
| P1-03 | P1-01 | PMC端面の仕様と実装 | Hφ=0の本質境界を正しく扱い、軸境界と混同しない。解析比較 |
| P1-04 | P1-02 | Epkの収束と角部診断 | 丸め半径固定の収束、特異角では未収束表示。見た目の平滑化と数値を分離 |
| P1-05 | P0-01 | 外部実機参照データ | provenance付きshape、BC、材料、規約、参照値、誤差。取得不能なら未完 |
| P1-06 | P0-03 | 出力の原子的完了処理 | 一時出力から完了状態へ移行。途中失敗を成功と判定しない |
| P2-01 | P1-01 | mode tracking | パラメータ掃引で近接/交差モードを重なり行列で追跡 |
| P2-02 | P2-01 | sweep/tune | 固有場を追跡し、目標周波数へ収束後に細かいメッシュで再検証 |
| P2-03 | P1-05 | active length・ビーム位相指定 | 入力schema更新、旧case移行、TTF規約のテスト |
| P2-04 | P2-02 | Python notebook/GUI | 同じcaseとsolver APIを使い、再現可能な保存を提供 |

## P0-02 の実装メモ

Gmshを最初から必須Python依存にせず、タグ付き中間メッシュを取り込む方法を先に検討する。
Gmsh `.msh` を読むなら版と対応要素を限定して明記し、タグが不明な境界をPECだと推定しない。
任意輪郭を許可しても、初期TM空間の軸連結・単連結等の仮定を破るケースは拒否する。
メッシュの座標 `(r,z)` とcaseの点列 `(z,r)` の違いに注意。

## 次の物理機能は別仕様にする

TE0np: Eφを未知とする別の境界条件とBessel零点を検証する。
同軸TEM: 内導体と多重連結領域、静的nullspace、端面条件を導入する。
静電/静磁場: eigenproblemの係数を変えるだけにせずsource problemとして設計する。
m>0/3D: nodal P1ベクトルの流用を避け、混合空間・H(curl)・nullspaceの扱いを決める。
