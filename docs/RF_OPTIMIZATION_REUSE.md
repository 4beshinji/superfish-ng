# RF探索の実行内再利用

2026-09-09、基準9fe3ff2。対象はD03の既存2変数探索の検証費用。[数値仕様](RF_OPTIMIZATION.md)を変更しない。

## 受入条件

- 同じ探索実行内では、確認済み祖先の追跡とRF設計評価を繰り返さない。
- 新規実行・保存再開でキャッシュを共有しない。再開文書は最初に全試行を保存場から再構築し、全文書を比較する。公開replayにも永続キャッシュを導入しない。
- 入力要求、祖先順、実装hash、3水準の全nativeジョブファイルを照合し、変更・欠落・リンクを拒否する。新しい試行の評価中の祖先変更も保存前に検出する。
- 保存文書、探索順、追跡、五量評価、採用値、予算、最終細分は従来版と完全一致する。FEMの置換や数値許容差の変更をしない。
- 同じ保存済み4試行の1→2→3→4試行の評価を変更前後で比較し、評価回数と時間を記録する。時間はこの環境の観測であり、一般形状の性能保証ではない。

## 実装方針

既存自作の曲線適応計算の実行内prefix検証方式をRF探索へ適用する。キャッシュは実行開始時に生成し、完全検証した試行と元ファイルのhashを深いコピーで保持する。各追加前に元ファイルを照合し、追加した候補だけを再構築する。最後に全水準の全ファイルを再照合する。新しい水準は通常のnative read_job/read_solutionとProject照合を通す。

従来の終了時の二重native再読込は、検証開始時からの全ファイルhash一致で置き換える。公開replayは全試行のnative検証・追跡・RF評価を引き続き実行する。API・保存書式・GUI操作は変えない。

## 保存済み4試行の対照

変更前 `out/rf-optimization-prefix-before-20260909` と変更後 `out/rf-optimization-prefix-after-20260909` は、既存CLIの `out/rf-optimization-independent-20260909/resumed-1/checkpoint-004.json` の祖先を使った。同じ1/2/3/4試行の文書全体が各段階で完全一致した。最後の文書も既存CLI保存値に完全一致する。新しいFEM実行ではない。

| 指標 | 変更前 | 実行内再利用 |
|---|---:|---:|
| RF設計評価の累計回数 | 10 | 4 |
| 4段階の観測時間 | 220.994秒 | 109.735秒 |

両方ともテスト等が並行して動いていた単一観測で、並行負荷も同一ではない。時間比約2.01はこの測定の結果だけを表し、一般的な速度保証やFEM自体の高速化を意味しない。評価回数の削減と文書完全一致を独立した受入条件とする。各実行の開始/終了時にソース不変を確認した。変更前と変更後のソースhashが同じという主張ではない。

## 新規FEMによる確認

`out/rf-optimization-reuse-independent-20260909` は修正後のAPIで4試行・12水準を新規に計算した。半径/軸方向の試行値は `[1,1] → [1.01,1] → [1.01,1.01] → 最終同値`。公開replayの全文書再構築と一致し、既存CLIの全12水準のProject/modes、試行列と終了判定にも完全一致した。

最終球形の独立解析に対する相対誤差は、f 2.472e-5、加速器R/Q 2.525e-6、G 1.077e-7、Epk/Eacc 2.314e-4、Bpk/Eacc 6.077e-6。既存許容差は順に1e-4、0.005、0.005、0.01、0.01で全てPASS。解析式は検証専用で、製品のFEMを置き換えない。

## 回帰テストの役割

変更前のRF探索5件は386.355秒でPASS。追加のprivateキャッシュ保護テスト1件は、合成ファイルによるコピー分離、要求/祖先/実装変更、内容変更・削除・追加・同じ内容のリンク置換の拒否を確認する。これはFEM結果の検証そのものをmockで代用した主張ではない。

既存の実FEM保存再開テストには、再開時の祖先1試行＋追加2試行でRF評価が3回だけであること、公開replayでは全3試行を評価すること、新候補の実RF評価後に祖先Projectのバイト列が変わると保存前に拒否しキャッシュと以前のcheckpointを保持することを追加した。

最終標準 `out/validation-rf-optimization-reuse-20260909` は743件中741合格・参照環境専用2件skip（1161.975秒）。追加した祖先変更/回数検査も通過。seed9モード19量の周波数差0、RF最大相対差8.882e-16。標準・新規独立計算・変更後計測・終了後431対象hashが一致する。driver/logを各outへ保存。全関連実行は終了。新規GUI/ブラウザー・Wine・Hosted CI・他OS受入は行っていない。D03/全体の一般変数・一般性能・追加物理等は未完、親集計8受入/8進行中/16他未受入/1候補=33を維持する。
