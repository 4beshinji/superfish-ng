# S05: 検証済み磁気後処理量のGUI受渡し

2026-09-13 JST。固定871sourceを主877sourceへ統合し、専用API範囲で限定受入。

専用画面 `/magnetic.html` で、明示された元nativeフォルダーと保存報告を取り込む。平面多極版1/2、平面力版1/2、正半径の軸方向力版1を各専用APIで厳密に再計算する。元5ファイルと報告を新規Jobへ所有コピーし、提出時/取込前後のSHA、全FEM replay、実装の安定性を照合して最後に完了manifestを公開する。既存JobManagerの非同期ワーカー・中止・中断/再起動を共有する。

表示前の全FEM再検証は読取専用の非同期処理で行う。サーバー内の検証済み全ファイルSHAと表示値だけをキャッシュし、毎回の表示/ダウンロードで一致を確認する。再起動でキャッシュは消え、再度全FEMを検証する。元nativeや報告の改変、リンク、不正な形式/版・再ハッシュされた数値改変、未知actionフィールド、指定外ファイルのダウンロードを拒否する。

多極は原点[m]・半径[m]・局所軸回転[rad]、normal/skew[T]と明示規約、元4標本系列のRMS/再構成/負次数/打切り診断を表示する。平面力はFx/Fy[N/m]、重み付き応力トルクと節点回転の応力トルク[N m/m]を別欄に表示する。軸非接続は全周Fz[N]とポテンシャル[J]であり、平面量に換算しない。元Case/材料・全変位/係数SHA・ポテンシャル・反復履歴も保持する。

仮想仕事nullは未実施、完了した差分は計算完了、保存された実非線形失敗は失敗として表示する。失敗した対のnullを零に置き換えない。Jobの完了は検証/取込の完了であり、場・力の連続誤差の合格を意味しない。詳細JSONではIEEE負のゼロも保持する。報告、取込要求と元5ファイルを、検証済みの元バイトのまま保存できる。RF履歴から専用画面へ遷移し、RFモードの選択には磁気報告を入れない。

追加Job 6unit、GUI 6unit、capability 3unitを確認した。初回Job試験では既存fixtureの戻り値数を誤認して5件の準備が失敗し、アダプターだけを修正後6件36.656秒でPASS。初回GUI 6件はPASSしたが、GUI操作をCLI用operations欄へ入れたことで既存CLI helpの4subtestが失敗。専用gui_actions欄へ修正し、該当GUI 1件と能力表3件を9.064秒で再確認した。元ログと判断はout/magnetic-report-gui-development-20260913に保持する。

独立最終20例は、実取込worker 20、初回非同期replay 20、再起動replay 20、140ダウンロードを確認。多極6、仮想仕事未実施6、完了6、実求解失敗2。全表示値/規約/診断・元Case/履歴を保存参照と照合し、元120ファイルと所有180ファイルが不変だった。最終証拠はout/magnetic-report-gui-independent-navigation-trial-20260913/report.json。初期/待機修正/負のゼロ修正の独立出力も別ディレクトリに保持し、最終sourceへ差し替えない。

Chrome実操作は初回28項目、同じワークスペースを新しいセッションで開き直した26項目がPASS。各20報告の画面値・単位・全詳細JSONと140ダウンロードが一致。認証/未知フィールド拒否、所有コピーの改変時の表示停止、不正報告の実worker拒否、RF履歴遷移、既存RFの実pillbox FEMと全native RF量、平面/Hφ画面を確認した。再起動試験は既存RF結果を再読込したもので、再度RFを解いたとは扱わない。

最初のChrome試験は遅延スクリプト初期化前の操作で停止。次に実報告中の二つの-0が標準JSON.stringifyで+0になったことを厳密比較が検出し、詳細表示シリアライズだけを修正した。その後の履歴遷移でも同じ初期化待機を補完した。数値計算・比較条件・許容差を変更せず、失敗出力を残して新しい出力で再検証した。最終ブラウザー証拠はout/magnetic-report-gui-browser-navigation-20260913とout/magnetic-report-gui-browser-restarted-20260913。外部HTTP要求なし、対象実装SHA不変。初期試験が残した失敗/改変Jobは陰性対照として保持する。

標準はout/validation-magnetic-report-gui-candidate-20260913。受入計画は[GUI受渡し](S05_GUI_HANDOFF_PLAN.md)。磁気Case編集・包括的GUI求解は対象外。次の[原要件照合](S05_ACCEPTANCE_PLAN.md)、対象版確認と未対応な軸接続/B-H/反跳の軸対称力、親S05/全計画は未完。新規依存・外部資料・旧版実行なし。

独立最終20例は128.764624秒。実ブラウザーはChrome/153.0.8010.36。元Python APIの試験をブラウザー試験と読み替えていない。

標準3326.087秒、1389合格・任意NGSolve参照2件/HTTP環境1件skip、ResourceWarningなし。主12unitは86.631秒でPASS。標準/独立の固定871sourceと主877source（不変egg-info 6件）は完全一致。独立全例を主で再実行したとは扱わない。旧ベンチマーク・許容差不変。統合証拠は標準出力内seed_regression.json。
