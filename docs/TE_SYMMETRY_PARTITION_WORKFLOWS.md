# TE対称形状のメッシュ交換・分割切替

2026-09-15 JST：[TE分割切替のGUIと実円弧検証](TE_PARTITION_GUI_AND_CAP.md)を限定受入。
調整版8 GUI12/実円弧Study版4 GUI8項目PASS。円弧調整は比較予算300000で停止し、別要求700000で保存再開・再生後TUNED。
新15半領域FEM（初回停止の3場を含む）、13組104配列/全RF一致、289 nativeファイル保持。物理閾値・製品コードの変更なし。
全handle終端。TE計画自動生成・他物理/D03等は残り、親33=10/16/6/1、全goal ACTIVE。

開始HEAD `8499503`、2026-09-15 JST。D02のTE対称形状について、Study版4と調整版8の分割切替を接続する。
受入条件は同じ対称セクター・元半領域の参照座標・全鏡映体積・実Eφの保持、
値に応じた分割選択、保存再開と元分割での最終細分、独立体積/円筒周波数とnative証拠である。

Study版4と調整版8の分割指定は異なる。
Study版4は同じ表現P2境界を持つ初期メッシュへ交換し、元Project由来の比較写像を使う。
調整版8は各分割の明示参照座標に基づき、独立接続・境界分割とその履歴を対応付ける。
解析曲線が同じだけではP2境界が一致するとは限らないため、Study版4の全二次境界一致検査は保持する。

TEでは既存の同一セクター・最大1対称面・固定RFの検査へ接続する。TMの閉PEC制限は維持する。
交換先・各分割の全細分段階で鏡映幾何も検査する。
調整版8の参照座標は元半領域を記述するため、その被覆検査には反射指定を外したProjectを渡す。
共有の選択転送関数の制限は変更していない。返すProjectは鏡映指定を保持し、
場の追跡は既存の両側実Eφ・正の体積重みの処理を使う。
局所細分の選択は交換先ごとに固定し、元メッシュの選択番号を継承しない。

新テストは円筒側壁に中間頂点を持つ3要素の元メッシュと、軸辺を分割した4要素の交換先を用いる。
値の増加では側壁中間半径が変わる。折線の合成形状であり、測定された構造ではない。
境界切替の正側採用と、元の3要素分割へ戻る最終細分を確認する。
8条件（z_min/z_max、電気/磁気対称、直接/鏡映）の半体積を独立な円筒公式と照合し、
鏡映P2写像の体積積分がその2倍になることを別に検査する。
変形後の体積は円錐台公式、初期磁気対称円筒の周波数はJ1の第1零点と軸方向波数 `k_z=π/(2L)` から求める。
既存の拒否を先に再現した。最初のテスト補助にはquadratureの誤importがあり、修正後に体積検査の8条件で拒否を再確認した。

新4unitは221.740秒、追加の局所細分1unitは0.464秒でPASS。全8条件の幾何、
Study実場/体積/保存再生、調整の切替/保存再開/元分割の細分、改変拒否と事前拒否を確認した。
旧テストの「TEそのものを拒否する」期待は新機能の受入へ変更し、RF指定と不正scheduleの拒否は残した。

専用記録は `out/te-symmetry-partition-20260915/`。
独立Study2、逐次CLI2、適応CLI2、解析比較用pilot1、調整CLI3の計10半領域固有値計算を実施した。
逐次は1点で保存して別出力へ再開。適応は1区間を採用して元2点へ到達。
調整は異なる4要素分割を含む2試行で保存し、親試行0の3要素分割を細分してTUNED。
元2試行のhashを保持し、CLI保存再生も成功。両判定の要求は1 MHz、粗細差634,601.819044 Hzで両方true。
この操作要求や2メッシュ差を離散化誤差の上限と見なさない。
磁気対称円筒の解析周波数2,405,059,947.968348 Hzに対してpilotは2,405,741,104.626683 Hz、
相対差2.83218162e-4。事前に置いた5e-3の解析照合基準を満たした。

独立Studyと逐次/適応CLIの4組・32 NPZ配列と全RF量が完全一致した。
調整の全試行でTE非該当の両R/Q、鏡映元セクター、実Eφを確認。
117 nativeファイルの保持、元試行のhash、全鏡映体積の円錐台公式との一致を確認した。
API/CLIは実FEMを用い、解析式を固有値計算の代わりには使用していない。

単独の`remesh-curved-project` CLIも、APIと完全に同じProjectを出力した。追加固有値計算0。
今回のAPIは明示した置換計画を受理する。`generate-curved-remesh-plan`は従来どおりTM限定であり、
TE向け計画自動生成を接続済みとは扱わない。実円弧の分割切替と専用GUIの検証も残る。

関連27unitは932.594秒でPASS。
対象は `test_curved_project_remesh`、`test_curved_partition_schedule`、`test_curved_remesh_study`、
`test_partition_tuning`、`test_te_partition_tuning` と
`test_te_symmetry_harmonic_workflows.TESymmetryHarmonicWorkflowTests.test_fixed_rf_and_invalid_mesh_schedule`。
既存TMの交換・境界拒否・履歴・品質/予算、Studyの値選択/追跡/適応再開、直接閉PECのTE調整を含む。

変更は幾何の要求検査と交換/分割の事前検査に限定し、固有値・場・RFの計算本体は変更していない。
影響を上記の利用先へ限定できるため、full/seed validatorは再実行していない。
過去のfull結果を今回の全受入結果として再表示することもない。hosted CIは実行していない。
336製品ファイルのSHAは専用実行開始時と最終確認時に一致。新規・関連検証と専用実行のhandleは終了0で終端を回収した。事前の拒否再現ログは別に保持した。
[集約結果](../benchmarks/tuning/te-symmetry-partition-20260915.json)を保存した。
親D02・D03と全計画goalは継続する。


