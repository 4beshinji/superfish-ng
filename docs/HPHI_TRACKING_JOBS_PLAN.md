# Hφ部分空間対応の所有保存・worker・CLI

同領域のE/H対応APIに続く操作工程。[実装記録](HPHI_TRACKING_JOBS.md)の範囲で限定受入。
前後の完全なHφ nativeまたは管理済み単発ジョブを受け取り、元係数とProjectを所有コピーして比較する。

受入条件：

- kind=hphi_trackingの専用ジョブとCLI実行/再生を接続する。要求と両比較メッシュ、前後native各5ファイルとProject、元出所のhashを保存する。管理済みProjectの表示単位/規格化/加速経路を保持する。
- コピー前後の元入力hash・投入hash・worker claim・実行中の入力/実装変化・完全manifestを確認する。原保存を変更せず、元パスを移動後も所有コピーだけで完全再生できる。
- 再生では両最低正スペクトル・E/H場・比較区間・対応を再計算する。hashを更新した結果/ID/kind改変、リンク、欠落、要求と比較メッシュの不一致を拒否する。
- 実行完了completeとPASS/UNVERIFIED・個別ID完了を別々に保存する。上端guard未達・縮退・部分空間ID集合を維持し、元係数を新規固有解として作り替えない。
- 実workerの取消/再起動、CLIとの全文書一致、原native/Project不変、通常Hφ・細分差・他物理ジョブとの識別を確認する。標準と旧f/RF、固定sourceと主への統合を照合する。

GUIからの候補選択・結果/群表示・元場取込、履歴連鎖・形状写像は後続工程。新規外部資料・依存・旧版参照なし。

標準1071件、独立10worker/34再起動/10CLI、主3unit/3CLIがPASS。主638sourceへ統合済み。
