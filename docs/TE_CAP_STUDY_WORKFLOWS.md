# 実円弧TE逐次・適応StudyのCLI・GUI

開始HEAD `a935703`、2026-09-15 JST。D02の円柱部＋円弧端部、TE磁気対称z_min鏡映例を対象とする。
受入条件は元目標 `[0, 0.5, 1]` の保持、一時停止と保存再開、両側の実EφによるID追跡、
元対称セクター順位、未確認時の未到達点保持と再開禁止、独立Studyとの全保存配列・RF一致。
記録は `out/te-cap-studies-20260915/` に保存する。

入力と独立比較元は[座標別境界修正の実曲線Study](CURVE_COORDINATE_BOUNDS.md)を再利用する。
曲線・接合許容差1e-14 m・uniformレベル1・固定RFは同じ。
同じ実装・入力に対する独立Green体積増分と全鏡映体積の前段の検査を再利用する。
今回の変更はブラウザー検証器のみで、製品コード・物理・数値閾値・依存の変更はない。

検証器の逐次完了点数は固定2点から要求点数へ変更した。
適応停止ケースには `--stopped-request` で明示した入力を渡せるようにした。
従来のアフィン検証用既定ケースは保持する。今回の停止入力は元の形状則を保持し、
minimum_overlapだけを0.8から0.999999へ厳しくし、max_depthを0にする。
これは重なり判定による意図した拒否の検査であり、実縮退や連続枝の証明ではない。

CLIでは逐次1点/適応1試行で停止し、別出力へ再開して両者COMPLETE。
停止ケースはUNVERIFIED・終了値1となった。最初の実行補助スクリプトは全終了値0を期待していたため
最後にCalledProcessErrorを記録したが、要求された停止とnative出力は完成している。
補助スクリプトの期待終了値を修正し、既存の結果を使って後段検査を行う。再計算はしていない。

逐次GUIでは最初の6項目が成功し、完了済みジョブの結果読み込みで検証器の180秒上限に達した。
読み取り診断ではジョブCOMPLETE・画面busyを確認している。古い改変拒否メッセージは画面に残っていた。
`--result-timeout-ms` を正の整数として指定できるようにし、既定180秒を維持した。
保存済みcheckpoint-003を新しいブラウザーで600秒上限で再生し、1項目が成功した。
JSON完全一致、TE-fundamentalの実Eφ履歴、再開禁止、元3点の表示画像を確認。
追加固有値計算0。初回reportの失敗は保持し、8項目一括成功とは扱わない。

適応GUIは10項目PASS。重複キーのworker起動前拒否、要求の完全一致、初回1試行で停止、
保存テキスト一致・再生、改変拒否、再開後の3点採用とEφのID、完了再生、停止時の未到達点と再開禁止を確認した。
完了と停止の実画像も確認。停止では元目標1/2を未到達として残し、値0.5の試算を未採用として保持した。

CLI/GUIの通常3点ずつ・停止2点ずつ、計16の新しい半領域固有値計算を行った。
独立APIの既存3解を比較元に使い、16組の128 NPZ配列と全RF量が完全一致した。
重複を除く247 nativeファイルのhash保持もPASS。元要求・両側Eφ・元セクター順位を確認した。
CLI再開前の逐次1点/適応2点と計39ファイルのhashを別途照合した。
336製品ファイルのSHAは全ブラウザー開始時・補完開始時・最終時に一致。外部HTTP要求0。
全5GUIジョブはcomplete、専用サーバーSIGINT終了0、3ブラウザーとnative照合の終了を回収した。
CLI補助プロセスも終了済み。事前の自動集約待機は初回逐次reportの失敗で停止したため、
補完reportを含む分割受入を明示的に照合して集約した。失敗記録を書き換えていない。

変更範囲は検証器と記録のみ。全unittest・full/seed validatorは再実行せず、
[前段のfullと独立物理検証](CURVE_COORDINATE_BOUNDS.md)を再利用する。hosted CIは実行していない。
[集約結果](../benchmarks/tuning/te-cap-studies-20260915.json)を保存した。
TE対称の分割切替Study版4/調整版8、他物理/D03等は残り、全goal ACTIVE。

