# G03 曲線幾何と曲線要素

## 仕様の段階

G03は円・楕円・対象版で確認された双曲線と接線接続、曲率・最小丸め半径を扱う。
K02のNT=2/3の種別記録を、旧形式の全パラメータ/既定値の確認とは扱わない。
native数学表現を先に定義し、旧形式との対応はC00/C02の版付き証拠と照合する。

幾何は解析曲線を保持し、曲線パラメータとFEM次数を分離する。
次の段階をそれぞれ検証してから製品経路へ接続する。

1. 円/楕円/双曲線の点・接線・曲率と弦誤差上限、面積モーメント。
2. 線分/曲線を結ぶ境界の接線連続性、正半径/軸接続、交差/接触/微小隙間検査。
3. 元の解析曲線と許容弦誤差を保存した一般輪郭/Case/GUI連携。
4. 曲線要素の座標写像・正Jacobian検査・写像に一致するFEM積分/場/RF/保存。
5. 幾何細分とFEM細分を独立に変える周波数/場/RF検査。

解析曲線を折れ線へ変換できても曲線FEMの完成とは扱わない。
二次写像を導入する場合も一般の楕円を厳密に表すとは呼ばず、写像による幾何誤差を評価する。
現行のP1/P2直線要素と旧arc_profileの結果は保持する。

## 楕円弧コア — 2026-09-08

`conics.EllipseArc(center_zr_m, semiaxes_m, start_rad, sweep_rad, rotation_rad=0)`。
局所点 `(a cos θ,b sin θ)` を(z,r)平面で回転・平行移動する。a/bは正の半軸長、
回転と正の掃引は(z,r)で反時計回り。θ=start+sweep*t、0≤t≤1。
原点/回転角は2πで正規化し、掃引は非零・絶対値2π未満とする。
この開弧単体の中心や評価点を物理的な真空領域として認定しない。

`evaluate` は点、進行方向の単位接線、非負の曲率を返す。
速度の大きさ `s=sqrt(a² sin² θ+b² cos² θ)` より曲率は `ab/s³`。
最小曲率半径は区間端とθ=kπ/2の候補を全て検査して求める。
`signed_line_area_m2` は開弧に沿う1/2∫(z dr-r dz)。閉領域の面積には他の辺の寄与も必要。
center=(zc,rc)、端点差Δなら `(ab*sweep+zc*Δr-rc*Δz)/2`。

`linearize(tolerance_m,max_segments=20000)` は両端を含む点列を返す。
局所二階微分のノルムはmax(a,b)以下で、回転・平行移動でこの上限は変わらない。
区間長hの線形補間誤差は二階微分を積分するGreen核の絶対積分上限h²/8から
`max(a,b)*h²/8` 以下。曲線点と同じパラメータの弦上点との距離を抑えるため、
曲線から弦への最短距離もこの値以下になる。
この保守的上限から必要分割数を決め、上限を超える指定は明示拒否する。

`tests/test_conics.py` の独立不変量:

- 楕円の陰関数=1と、その勾配に対する単位接線の直交。
- 主軸端の曲率、円での一定曲率1/R、最小曲率半径。
- 正逆の点一致/接線反転、回転した四分楕円の面積πab/4と内接多角形の片側収束。
- 1e-6〜1e6倍の寸法変換と、各弦内5点の距離が指定誤差以下。
- 不正軸/角/パラメータ、分割数上限の拒否。

外部コードや新規依存は使わず、パラメトリック微分と積分から独立実装した。
G03.Sは進行中、Iは楕円弧コア、Vはこのコアのみ。双曲線、接線接続、
交差/隙間保証、Case/GUI、曲線FEMの全体受入は未完了。
標準196件中194合格・2 skip、`out/validation-g03-ellipse-core-20260908/validation.json`
PASS。既存円筒/非円筒の全mode量とCase hashは不変。

## 双曲線弧コア — 2026-09-08

`HyperbolaArc(center_zr_m, semiaxes_m, start_parameter, end_parameter,
branch=1, rotation_rad=0)` を追加。局所点は `(branch*a*cosh(u), b*sinh(u))`。
branchは整数±1、a/bは正の長さ、uは有限の異なる両端間を進む。
回転・平行移動は楕円と同じ。無限に伸びる枝全体を一つの弧として入力しない。
native表現であり、旧NT=3のパラメータ解釈を確定したものではない。

速度は `sqrt(a²sinh²(u)+b²cosh²(u))`、曲率の大きさはab/速度³。
速度は|u|に対して増加するため、区間の0に最も近い点で最小曲率半径を評価する。
開弧の1/2∫(z dr-r dz)は、中心項に`branch*ab*(u_end-u_start)/2`を加えたもの。
二階微分のノルムも|u|に対して増加するので、区間最大|u|から弦誤差上限M*h²/8を求める。
楕円と同じ分割数上限と正の有限誤差指定を使う。

両枝の `(z/a)²-(r/b)²=1`、その勾配との接線直交、頂点半径b²/a、
反転・回転・線積分の多角形近似、寸法変換、各弦内3点の誤差を検証した。
双曲関数が浮動小数点範囲を超える区間や、曲率が表現できない評価は明示拒否する。
円錐曲線評価では楕円も含めて非有限の点/接線/曲率および非正曲率を返さない。

双曲線弧コアまで実装したが、接線接続・曲線境界の交差/隙間保証・Case/GUI/曲線FEMは未完。

## 線分と接続検査 — 2026-09-08

`LineSegment(start_zr_m,end_zr_m)` を追加。有限で異なる端点を持ち、線形点評価と
進行方向の単位接線、ゼロ曲率、無限の曲率半径、線分の面積寄与を返す。
弦近似は両端だけ。線分の無限曲率半径は未測定値ではなくゼロ曲率の表現である。

`check_curve_join(first,second,position_tolerance_m=...,angle_tolerance_rad=1e-8,
require_tangent=True)` でfirst終点→second始点の接続を検査する。
位置許容差は明示の非負SI長。位置差を先に判定し、次に単位接線の外積/内積から
atan2で0〜πの向きの差を求める。逆向きの平行接線は滑らかな進行と認定しない。
角許容差は0以上π/2未満。明示した角接続ではrequire_tangent=Falseとし、測定角と
接線連続の真偽を返す。位置差・接線角は別のエラーとして報告する。

これは進行方向の幾何的接線連続（G1）であり、パラメータ速度の一致（C1）や曲率連続を
要求しない。許容差内でも端点を移動/接着しない。閉輪郭としての整合や離れた曲線間の
交差/隙間はまだ別途検査が必要で、この接続関数単体の合格で認定しない。

独立検証は主軸端の線分↔楕円と楕円↔回転双曲線の接線、逆向き拒否、端点隙間と
角の分離、元端点不変、矩形の線積分面積、不正許容差の拒否。
標準202件中200合格・2 skip、`out/validation-g03-joins-20260908/validation.json` PASS。
次は曲線区間の位置/誤差包絡と、閉輪郭の交差/軸/隙間検査へ進む。G03全体は未受入。

## 曲線区間の包絡と離隔 — 2026-09-08

`curve_bounds.curve_bounds(curve,first=0,last=1)` は指定区間の(z,r)下限/上限を返す。
楕円の各座標A cos θ+B sin θについて微分零点θ=atan2(B,A)+kπ、双曲線の
A cosh u+B sinh uについて存在する微分零点u=atanh(-B/A)を端点に加えて評価する。
線分は端点だけ。逆向き・回転も係数と区間に反映する。
計算値には座標/中心/半軸のスケールに対する128 epsとnextafterの外向き余裕を加える。
これは倍精度での工学的な保守判定であり、超越関数の形式検証済み区間演算ではない。

`certify_curve_separation(first,second,minimum_gap_m=0,max_boxes=10000)` は区間対の
包絡箱が指定離隔より遠ければその区間対を合格とし、未分離なら大きい箱側を二分する。
全区間対を分離できた場合だけseparated=Trueと距離下限を返す。
端/中点の組合せで距離不足を検出した場合はFAIL、探索上限や浮動小数点分解能で
判定できない場合はUNVERIFIEDを含むエラーとする。未確認を交差確定や離隔合格にしない。
有効な共有端点を持つ隣接辺はこの検査には合格しないため、隣接辺用の契約は別途必要。

検証は円弧内部の解析極値、端点では見逃す軸横切り、回転/逆向き区間の点群包含、
平行線の既知距離、交差/共有端点の拒否、同心円弧での上限UNVERIFIEDと再細分成功。
次はこれらを閉輪郭へ組み合わせ、共有端点近傍と離れた辺をそれぞれ適切に検証する。

## 共有端点近傍の検査 — 2026-09-08

`directional_derivative_bounds` で指定方向への位置微分（fractionに関する微分）の
上下限を評価する。回転楕円の三角関数、双曲線の双曲関数の極値候補を評価し、
前節と同じ倍精度の余裕を加える。単位接線のサンプリングだけで単調性を推定しない。

`certify_adjacent_curves(first,second,position_tolerance_m=...,max_depth=32,max_boxes=10000)`
は端点接続を確認し、両接線の和方向への微分が双方で厳密に正となる近傍を探索する。
その近傍では進行方向の投影順序から再交差を除外する。残りの区間対は全て通常の
離隔検査で確認する（separation関数へ部分区間指定を追加）。
端点が許容差内でも投影順序が逆転する場合は、重なりを隠す可能性があるためUNVERIFIED。
逆向き接線、単調近傍未確認、探索上限でもUNVERIFIEDとし、座標を修復しない。
角接続も投影単調性があれば扱える。接線連続を要求するかは別の接続契約で指定する。

位置微分を独立中心差分で確認し、線分→楕円・楕円→線分・直角線分、逆向き、
端点許容差内の実重なり、探索深度不足を検証した。
標準207件中205合格・2 skip、`out/validation-g03-adjacency-20260908/validation.json` PASS。
次は単一閉輪郭へ統合し、軸連続性・辺タグ・全辺対と幾何モーメントを検証する。
G03のCase/GUI/曲線FEMおよび全体受入は未完了。

## 単一の曲線閉輪郭 — 2026-09-08

`curved_contour.CurvedContour(curves,edge_tags,join_tolerance_m,minimum_gap_m=0)` を追加。
線分/楕円/双曲線の有向列と各辺タグを保持する。少なくとも2プリミティブを必要とし、
始終点を含めて接続を検査する。位置許容差は明示し、軸長の1e-10を超える指定は拒否する。
これは物理的な隙間を大きい許容差で隠さないための上限で、形状の自動修復は行わない。

軸タグはr=0の直線だけに付け、1本の循環的に連続した辺鎖でz=[0,L]を隙間・重複なく
覆うことを要求する。全曲線の極値範囲でz=[0,L]と非負半径を位置許容差内で検査する。
対称タグはz=0/Lの直線端面だけ。r=0線分をPECとして紛れ込ませない。
解析線積分の和が正の輪郭（z,rで反時計回り）を要求し、向きは黙って変更しない。

検査用には各プリミティブを二分し、軸+半楕円のような2辺閉形状でも、隣接ペアごとの
共有端点が一つになるようにする。元の曲線列とタグは保持する。
隣接ペアは前節の共有端点検査、その他全ペアは通常の離隔検査を通す。
元から非隣接のプリミティブ間にはminimum_gap_mを要求する。元の同一/隣接曲線間は
共有点に向かって距離がゼロとなるため、人工分割で追加の物理隙間を要求しない。
未確認の辺対が一つでもあれば閉輪郭を受理しない。

独立検証は軸+半楕円の面積πab/2と寸法変換、分割軸と混在端面タグを持つ矩形面積、
交差・閉じない端点・軸/対称タグ誤り・下半平面・逆向き・過大許容差の拒否。
標準210件中208合格・2 skip、`out/validation-g03-closed-curves-20260908/validation.json` PASS。
次は解析曲線の弦近似とContourへの変換、幾何誤差/体積、Caseで元曲線を保持する契約。
曲線FEM、接線接続の自動構築、旧入力対応はまだ未完了。

## 解析曲線から弦Contourへ — 2026-09-08

`CurvedContour.linearize(tolerance_m,max_segments=20000)` は `ChordApproximation` を返す。
解析曲線列は変更せず、タグ付きContour、各弦の元曲線番号、各曲線の最大端点調整量、
元の解析面積と多角形面積差、弦に割り当てた誤差を保持する。
max_segmentsは輪郭全体に対する上限で、曲線ごとに上限をリセットしない。

曲線接続の浮動小数点差を多角形側で整合させる場合は、直線側の端点を優先し、
曲線同士では中間点を採用する。最大端点変位を先に誤差予算から差し引き、残りで
各曲線を弦近似する。端点変位だけで予算を消費する場合は拒否する。
元弦と調整後の弦の差は端点変位の線形補間なので、曲線点と弦の距離は
「元の弦誤差＋最大端点変位」以下になる。調整を無記録の形状修復として扱わない。

得られた多角形にはG01のContour検査を再適用し、向き逆転も別に拒否する。
曲線に交差がなくても粗い弦が不正になれば変換を受理しない。
半楕円の面積と、回転して得られる楕円体の独立体積4πab²/3への片側収束、
軸/PECと混在端面タグ、元曲線不変、分割上限と予算不足の拒否をテストした。
標準212件中210合格・2 skip、`out/validation-g03-chord-contour-20260908/validation.json` PASS。
次は元曲線・弦誤差・調整記録をCase/保存へ保持し、幾何細分とFEM細分を分ける。
解析体積の一般式、曲線FEM、接線自動構築と旧入力対応は引き続き残件。

## Caseと保存で元曲線を保持 — 2026-09-08

v3 `geometry.type=curved_contour` を追加。geometryはCurvedContourのcurves/edge_tags/
join_tolerance_m/minimum_gap_mと、必須chord_tolerance_m、任意chord_max_segments（既定20000）を持つ。
各curveはtype=line/ellipse_arc/hyperbola_arcと前節の同名パラメータを持ち、未知項目を拒否する。
端面条件はedge_tagsを正とする。PythonではCase.curved_contourとcurve_chord_tolerance_m/
curve_chord_max_segmentsを指定する。Case.contourは検証済みの弦多角形として導出する。
既存contourを同時に渡す場合は導出値との完全一致が必要。dataclasses.replaceで弦誤差を
変える際はcontour=Noneとして再導出する。元曲線を捨てて旧polygonへ偽装しない。

自動メッシュはG02のcontour_mesh設定で弦多角形を処理し、現行P1/P2直線要素で解く。
保存geometry_approximationに直線弦表現/geometry_order=1、元曲線対応、端点調整、
弦誤差配分、解析面積/弦面積/差を記録する。保存Caseから同じ曲線/弦/係数を再生成できる。
曲線FEM対応を意味しない。解析曲線の鏡映は未実装として明示拒否し、片側の元曲線を
全領域の保存情報へ流用しない。GUI・曲線の幾何細分Studyは次の接続段階。

半楕円CaseのJSON往復・P2保存/再読込/係数再現、未知curve項目/型・弦誤差欠落の拒否を検証。
標準214件中212合格・2 skip、`out/validation-g03-curved-case-20260908/validation.json` PASS。
次は解析体積・鏡映・GUI/Studyへ元曲線を接続する。曲線FEM/接線構築/旧入力対応は未完。

## 解析回転体体積 — 2026-09-08

`curve_moments.revolution_volume_contribution` は各有向辺の `-π∫r² dz` を返す。
Greenの定理から、反時計回りの閉輪郭で総和すると `2π∫∫r dz dr` となる。
CurvedContour.volume_m3はこの解析寄与の和。開いた1辺の寄与は負にもなり得る。
線分は円錐台多項式の直接積分。楕円はexp(iθ)、双曲線はexp(u)の次数±1の座標表現から
r² dzの次数±3までの積を作り、指数関数を解析積分する。弦近似は用いない。
三角関数側の短区間積分は中心位相とsincで計算する。

独立検証は円柱/円錐台、半楕円からの回転楕円体4πab²/3と寸法変換。
双曲線はz=-sinh(u), r=-cosh(u)の開弧でr²=1+z²をzで直接積分して照合する。
回転した楕円/双曲線の開弧は点座標だけを使う細分線積分とも照合した。

保存geometry_approximationへanalytic_volume_m3/chord_volume_m3/volume_difference_m3を追加。
解析面積/体積とメッシュ用多角形の幾何誤差を、FEM場の精度から分離して記録する。
標準217件中215合格・2 skip、`out/validation-g03-analytic-volume-20260908/validation.json` PASS。
鏡映・GUI/Study、曲線FEM/接線構築/旧入力対応は引き続き未完。

## 元曲線の鏡映 — 2026-09-08

CurvedContour.reflectedは1つの直線端面が同じ対称タグで完全に覆われている場合だけ
継ぎ目を取り除き、残る曲線列と鏡映後の逆順列を接続する。混在PEC/対称の継ぎ目、
複数端面/複数対称種別、分離した継ぎ目は拒否する。z_min鏡映では全体をLだけ移動する。
結果は閉輪郭・全辺対の通常検査を再実行して受け入れる。

楕円の鏡映と進行反転はrotation→-rotation、start→π-(start+sweep)、sweep保持で表す。
双曲線はrotation→-rotation、branch反転、区間両端の交換。中心と線分端点も鏡映する。
同値な三角関数表現の選択で端点丸め差が変わるため、区間検査の許容値を緩めず
両端接続が保たれる表現を使用する。元の半領域の曲線を全領域へそのまま流用しない。

既存P1/P2場の鏡映へ接続し、全Caseの弦Contourは全解析曲線から再導出する。
楕円のz_min/z_max×電気/磁気の4条件で面積/体積/エネルギー2倍、同じ全メッシュを使う
独立の全領域再計算とのf/RQ/G一致、保存/再読込を検証した。
鏡映されたモード番号は全領域のモード順位ではなく、既存の部分スペクトル規約を保つ。
標準218件中216合格・2 skip、`out/validation-g03-curve-reflection-20260908/validation.json` PASS。
GUI/Study、双曲線を含む追加の総合受入、曲線FEM/接線構築/旧入力対応は未完。

## 幾何細分とFEM細分のStudy — 2026-09-08

geometry_convergenceはcurved_contourの `/case/geometry/chord_tolerance_m` に対応。
元曲線・FEM最大辺長・次数を固定して弦誤差だけを減らす。mesh_convergenceは元曲線と
弦Contourを固定し、G02と同じく最大辺長を細分倍率で除算する。
物理形状照合では弦誤差と分割上限を離散化制御として除外するが、元曲線パラメータは保持する。
Studyの各点に保存済みgeometry_approximationを転記し、解析面積/体積と弦との差を追跡する。
P2を含むStudy結果をP1と表記していた箇所も修正した。表面ピークの精度認定は行わない。

半楕円で両細分の設定独立性、3段階の実行・保存・比較、楕円体解析体積と誤差減少を検証。
標準219件中217合格・2 skip、`out/validation-g03-curved-study-20260908/validation.json` PASS。
例 `examples/curved_ellipse.json` は弦誤差0.002 m、FEM最大辺0.02 mの粗い操作用入力。
弦誤差0.002/0.001/0.0005 mのStudy結果は
`out/study-g03-ellipse-geometry-20260908/study-results.json`。
体積誤差絶対値は1.02033e-4/4.56735e-5/2.57557e-5 m³へ減少したが、数値収束はFAIL。
処理完了を精度受入へ読み替えず、G03全体の収束検証ではさらに細分して確認する。

再現はインストール済み環境で次のAPIを使用し、未使用の出力先を指定する。

```python
from superfish_ng import Case
from superfish_ng.project import Project
from superfish_ng.studies import Study, execute_study
case = Case.load('examples/curved_ellipse.json')
study = Study(Project.from_dict(case.to_dict()), 'geometry_convergence',
              '/case/geometry/chord_tolerance_m', [.002, .001, .0005])
execute_study(study, 'out/ellipse-geometry-new')
```

次はGUIの元曲線読込・弦誤差編集・表示と、独立した幾何/FEM収束の総合受入を進める。

## 解析曲線のGUI接続 — 2026-09-08

受入範囲は元曲線の読込・保持、弦誤差/分割上限とFEMメッシュ制御の別編集、
計算用閉輪郭の表示、P2計算・保存・書出し再読込。
curved_contourはファイルで曲線を定義し、GUIは曲線パラメータを再構築しない。
プレビューは弦輪郭を描画し、解析面積/体積との差（弦 − 解析）を表示する。
境界タグ由来の端面表示と未使用profileメッシュ入力は編集不可とする。

実Chrome検査: out/gui-g03-curves-browser-20260908/report.json の5検査PASS。
examples/curved_ellipse.jsonを読み、弦誤差2→1 mmで輪郭点9→13、
最大辺長10 mm・最小角12度・最大4000要素・8反復へ編集してP2計算、
結果表示、元曲線を維持した書出し/再読込を確認。外部通信なし。
画面は同ディレクトリworkspace.png。標準220件中218合格・2 skip。
FEM実装は変更なし。曲線FEM、接線構築、旧形式対応、
独立した幾何/FEM収束の総合受入は引き続き未完。

## 独立した細分列の再現スクリプト — 2026-09-08

`scripts/validate_curved_refinement.py --out out/curved-refinement-new` は、
元の楕円を保持して次の2列を実行する。全点でP2、基本モード、最小角10度。

- 幾何列: 最大辺長5 mm固定、弦誤差0.5/0.125/0.03125 mm。
- FEM列: 弦誤差0.03125 mm固定、最大辺長20/10/5 mm。

周波数0.1%、RF各量1%、軸電場L2 1%、場重なり0.98の既存ゲートを維持する。
解析面積/体積との幾何誤差も保存する。これは細分間差の検査であり、
楕円体の独立電磁場参照解や表面ピーク精度の証明ではない。
各Studyの処理失敗を数値UNVERIFIEDとして記録し、もう一方の独立列を継続する。
比較ごとの表面場注記も実要素次数を表示するよう修正した。

最初の実行 `out/validation-g03-separated-refinement-20260908/` は幾何列2点目で停止。
12反復後の最小角7.60043279度で10度未達。メッシュを返さず、job.jsonに失敗を記録。
角度閾値を変えず `--max-rounds 24` による追加試行
`out/validation-g03-separated-refinement-round24-20260908/` を実行。
幾何列2点目は24反復でも最小角7.60043279度のままFAIL。単なる反復不足ではなく、
局所品質改善の停滞を次の修正対象とする。幾何列の数値収束はUNVERIFIED。

FEM列は両比較ともPASS。最終10→5 mm比較の相対差は周波数2.80237e-8、
R/Q 4.20720e-4、G 6.49333e-8、軸電場L2 3.78418e-4、場重なり0.999999999999。
弦体積差は全点で-1.666238791e-6 m³に固定され、幾何条件が変わっていない。
このPASSは固定多角形上のFEM細分に限定する。幾何列は未達でG03全体は未受入。
スクリプトは両列PASS時のみ終了コード0を返す。
標準220件中218合格・2 skip、validation-g03-refinement-report-20260908 PASS。
既存FEMの数式・数値閾値・ベンチマークは変更していない。

## 最大辺長内での対角線交換と停滞の切分け — 2026-09-08

improve_contour_anglesに明示max_edge_mを追加。指定時は、その長さ以内なら旧対角線より
長くなる交換も許す。最小角の厳密改善、正面積、境界固定、角近傍のサイズ制御は維持。
未指定のAPIは従来の「対角線を長くしない」規約を保つ。品質生成は最大辺長を渡す。

独立四角形(z,r)=(0,0),(1,0),(.2,1),(0,.5)では、対角線sqrt(1.04)から
sqrt(1.25)への交換が最小角10.49→24.78度を与える。最大1.1なら交換を拒否、
最大1.3なら許可する。境界・正面積・面積0.55・回転体体積23π/60の不変を検証。
これは無条件のDelaunay化ではなく、明示サイズ内の局所改善である。

out/validation-g03-bounded-flips-20260908/summary.json:
固定弦上FEM列はPASS。しかし幾何列0.125 mmは12反復後も最小角7.60043279度で失敗。
この変更だけで楕円の停滞が解消したとは扱わない。
追加診断では1反復後、(r,z)=(0,.15),(.0002187992153,.1516397294),(0,.153125)
の軸近傍三角形が7.6004/164.0194/8.3801度となった。
次は内点移動の候補不足を切分け、境界とサイズを維持する品質改善を検証する。
標準221件中219合格・2 skip、validation-g03-size-bounded-flips-20260908 PASS。

## 内点移動の停滞解消 — 2026-09-08

近傍平均へ向かう移動だけでは停滞するため、低品質の内点に方向探索を追加。
無次元の最小品質が0.4未満の点について、8方向、最近傍距離の1/2に
1,1/2,1/4,1/8を掛けた移動を既存の平均候補の後に試す。
0.4は探索対象を絞る計算量制御で、最小角の受入閾値ではない。
全候補は既存の正向き・最小品質改善・最大辺長・角近傍サイズ検査を通す。
境界点は移動しない。最終品質判定の10度条件は変更しない。

独立矩形の軸上を20分割した三角形扇で、内点が近傍平均と一致し旧平滑化は動かない。
方向探索で最小品質0.03204→0.09072へ改善。
境界/接続を固定し、正面積・断面積1・回転体体積πを検証した。

実行: `scripts/validate_curved_refinement.py --out out/validation-g03-directional-smoothing-20260908`。
幾何列はFEM最大辺長5 mm固定で、次のメッシュを生成した。

| 弦誤差 [mm] | 三角形数 | 最小角 [度] | 実測最大辺長 [mm] |
|---|---:|---:|---:|
| 0.5 | 7744 | 11.0611 | 4.99227 |
| 0.125 | 15781 | 13.8686 | 4.99490 |
| 0.03125 | 29007 | 10.1878 | 4.98614 |

0.5→0.125 mmは周波数相対差0.00239244でFAILを保持。
0.125→0.03125 mmは周波数5.93909e-4、R/Q 3.24149e-4、
G 2.26264e-4、軸電場L2 1.95165e-3で全既存ゲートPASS。
過去の12/24反復の品質失敗出力は保持し、成功結果で上書きしない。
この受入は楕円基本モードの直線P2・最終細分間差に限定し、曲線FEMや独立参照解の受入ではない。

固定弦FEM列も両比較PASS。最終10→5 mm比較は周波数2.10743e-8、
R/Q 5.65983e-5、G 3.04093e-8、軸電場L2 3.75723e-4。
標準222件中220合格・2 skip、validation-g03-directional-regression-20260908 PASS。
次は双曲線等の総合受入と独立の電磁場参照、曲線FEM/接線構築/旧形式対応を進める。

## 双曲線の閉領域・鏡映・製品経路 — 2026-09-08

examples/curved_hyperbola.jsonは合成形状 r(z)=a√(1+((z-h)/b)²)、
0≤z≤2h、a=0.05 m、b=0.08 m、h=0.05 m。両端と壁をPECで閉じる。
旧入力デッキや実測構造の再現ではない。zを変数とした独立積分は
A=a[h√(1+(h/b)²)+b asinh(h/b)]、
V=2πa²[h+h³/(3b²)]。弦は凸なr(z)の外側にあり、体積差は正で細分時に減少する。

半領域の両端×電気/磁気対称4条件で、鏡映後の解析面積/体積/Uが2倍、
全領域を別solveしたf/RQ/Gと8桁一致、保存再読込を確認。
寸法を3倍にした別solveでfが1/3、R/Q・G・TTFが不変、体積27倍も検証。
これらは対称性とスケールの不変量で、独立電磁場参照解とは区別する。

90度回転のcos値の丸め残差で半領域の接続がUNVERIFIEDになる不具合を修正。
正規化されたrotation_radが0,±π/2,±πと完全一致する場合だけ、
符号付き座標入替えを点・接線・包絡・モーメントに共通適用する。
近傍値は丸めず、nextafterによる隣接浮動小数点値では通常のsin/cosを維持。
接続許容差や離隔条件は緩めていない。

再現:
`scripts/validate_curved_refinement.py --case examples/curved_hyperbola.json --out out/hyperbola-refinement-new`。
実行はout/validation-g03-hyperbola-refinement-20260908/summary.json。
幾何はFEM最大辺5 mm固定、弦0.5/0.125/0.03125 mm。
FEMは弦0.03125 mm固定、辺15/7.5/3.75 mm。

| 最終比較 | 周波数相対差 | R/Q相対差 | G相対差 | 軸電場L2 | 判定 |
|---|---:|---:|---:|---:|---|
| 幾何 | 6.07479e-4 | 3.29396e-3 | 4.68036e-4 | 5.90920e-4 | PASS |
| FEM | 1.40582e-7 | 4.59187e-5 | 6.07844e-8 | 2.25565e-5 | PASS |

粗い幾何比較は周波数1.98059e-3、R/Q1.06041e-2でFAILを保持。
弦体積差は5.08173e-6→1.50850e-6→4.13609e-7 m³へ減少した。
実Chromeの5検査PASS: out/gui-g03-hyperbola-browser-20260908/report.json。
読込後に弦誤差0.5→0.25 mm、メッシュ制御編集、P2計算/表示/書出し再読込を確認。
標準226件中224合格・2 skip、validation-g03-hyperbola-domain-20260908 PASS。
独立電磁場参照、曲線FEM、接線構築、版付き旧形式対応は引き続き未完。

## 球形の独立電磁場参照 — 2026-09-08

[球形参照](SPHERE_REFERENCE.md)を追加。解析的な正則モードから周波数・正規化場・RFを求め、
円の弦輪郭P2との周波数、体積磁場/電場、軸場、R/Q、壁損失を独立比較。
弦誤差0.8/0.2 mmはFAIL、0.05 mmは全ゲートPASS。表面ピークは未認定。
これは球形の1モード・直線要素の参照受入であり、曲線要素を実装したことにはならない。
次は曲線要素の写像と正Jacobian・積分・場/RF/保存を分離して設計する。

## 二次幾何写像と局所行列 — 2026-09-08

[曲線要素契約](CURVED_ELEMENTS.md)に幾何/場/積分/保存の段階を定義。
6節点二次写像、det Jの全域最小値候補検査、物理勾配、局所TM行列を追加。
節点Jacobianが全部正でも間で反転する反例を拒否。
独立の曲線面積/回転体体積、一定場/線形物理場の行列二次形式、直線極限を検証。
全体メッシュ、Case/solve/保存/場/RF/GUIはまだ直線要素経路。次は境界の元曲線対応と
共有二次幾何節点・全体適合性の実装を進める。

## 元曲線区間と共有幾何候補 — 2026-09-08

弦ごとのfraction区間を保持/保存し、境界メッシュの頂点・共有辺中点を元曲線へ対応づける。
共有整合・軸/直線固定・全要素Jacobian/半径を検査。
楕円/双曲線の独立面積/体積改善と、曲線化で反転するケースの拒否を検証。
全体の境界交差/単射性検査は未接続。詳細は[曲線要素](CURVED_ELEMENTS.md)。

## 二次境界の検査 — 2026-09-08

共有幾何候補の二次境界をBezier凸包の再帰離隔と隣接接線投影で検査。
単純閉サイクル、折返し・交差・接触・予算不足を区別して受入/拒否する。
楕円/双曲線はPASS。端点多角形だけでは見逃す曲線交差と、
共有端点以外で交差する隣接放物線を拒否。全体空間/行列接続は次段階。

## 全体曲線空間/行列 — 2026-09-08

共有・向き・円板位相・内部を含む全二次辺の交差検査をCurvedSpaceへ集約。
全体疎行列を組み立て、球形の実験固有値が解析参照と相対9.33e-7で一致。
積分次数8/12も照合。[詳細](CURVED_ELEMENTS.md)。
場/RF/入力/保存経路は未接続なので、周波数の限定受入として記録する。

## 曲線解と体積場 — 2026-09-08

明示solve_curvedと参照要素座標からの物理場を追加し、指定エネルギー正規化を検証。
球形の独立参照に対し周波数/体積磁場/電場/エネルギーの各ゲートPASS。
RF・物理座標プローブ・通常保存は未接続。[詳細](CURVED_ELEMENTS.md)。

## 曲線RF積分 — 2026-09-08

曲線壁弧長の損失積分と厳密に直線の軸P2電圧を接続。
既存R01の区間/位相/active lengthと二つのR/Qを保持し、直線極限と球形参照で検証。
球形RF各量・壁積分次数ゲートPASS。表面ピーク・物理座標プローブ・通常保存は未接続。
[詳細](CURVED_ELEMENTS.md)。

## 曲線物理座標プローブ — 2026-09-08

減衰Newtonと参照三角形のBezier包絡細分を組み合わせ、
曲線の膨らみを含む物理座標の包含/逆写像を検査。
未確定を領域外へ読み替えず、共有辺は片側場を返す。
球形の独立物理点/全軸で場ゲートPASS。[詳細](CURVED_ELEMENTS.md)。
通常Caseの幾何次数と保存/再読込、ピーク/鏡映/Study/GUIは継続。


最小半径制約の接続（2026-09-08）: [minimum_meridional_radius_m](MERIDIONAL_RADIUS.md)を
完成曲線輪郭の任意指定として追加。PEC曲線内部の区間検査とPEC-PECのG1数値検査を課し、
構築/保存/鏡映へ保持する。既存の数値minimum_radius_mプロパティと認証の根拠は区別する。

## 極端な尺度での曲率・最小半径評価 — 2026-09-08

従来の楕円minimum_radius_mはspeed³/(a*b)を直接計算していた。
軸(2s,s)、s=1e±200では真の最小半径s/2が表現可能でも、中間積の範囲逸脱でNaNになった。
双曲線では軸(1e-300,1e-300)、u=460..461の半径/曲率が表現可能なのに、
比の中間積がoverflow/underflowして半径inf・曲率評価エラーになった。

積商の各因子をfrexpで仮数/二進指数へ分解し、指数を合算してからldexpで戻す処理へ変更した。
最小速度を選んだ後で半径を計算し、候補値の不要な三乗も作らない。
最終出力自体が正の有限floatとして表現できない場合は、範囲エラーを明示する。
表現できるsubnormalの最小半径は保持する。精度要求を緩めたり寸法を自動変更したりしない。

二進回転行列R=[[c,−s],[s,c]]について、曲率の分子にはdetR=c²+s²を含め、
未回転速度から計算する最小半径には|R|の一様尺度sqrt(c²+s²)を含める。
これらは浮動小数点評価であり、[最小半径制約](MERIDIONAL_RADIUS.md)の有理数区間証明とは別である。
点・接線・角度パラメータ・有限弧候補の数値評価方法は変更していない。

追加4テストは楕円の1e±200尺度、極端な双曲線の80桁Decimal独立参照、
異方的な軸の頂点半径b²/a、表現不能の明示拒否とsubnormal半径。
通常の形状/FEM精度や任意の極端な軸比を保証するものではない。
検証記録は[引継ぎ](CODEX_HANDOFF.md)。新規外部資料・依存は使っていない。
