# 平面線形磁静場の実FEM・元B/H・電流/磁束/エネルギー

2026-09-13 JST。[計画](PLANAR_MAGNETOSTATIC_SOLVE_PLAN.md)の固定715sourceを主721sourceへ統合し、専用API範囲で限定受入。
候補は `/tmp/superfish-planar-magnetostatic-solve-20260913`。

`PlanarMagnetostaticCase` は直線の単純多角形P1/P2、正値実数mu_r、全領域の符号付きJz[A/m²]、全境界の固定Az[Wb/m]または接線Ht[A/m]を要求する。接続した固定Az辺は同一IDにまとめる。少なくとも一つの固定Azが必要で、純Neumann/gaugeや暗黙の外部境界は拒否する。

境界の接線tは反時計回り、外向き法線nはtの右側。B=(∂yAz,−∂xAz)からHt=−ν∂nAz、ν=(1/mu0)/mu_rとなる。K=∫ν gradNi·gradNj dxdy [m/H]、体積荷重∫Jz Ni dxdy [A]、境界荷重−∫Ht Ni ds [A]を用いて固定自由度を消去し、実疎行列系を解く。最初の固定Azを基準とする相対係数と絶対Azを別に保持し、定数gauge移動で場を変えない。

元係数からAz、Bx/By[T]、Hx/Hy[A/m]を計算する。界面プローブは最小の元セル番号側のmu_rと領域・材料を返し、平均化しない。座標・セル・重心座標の混在boolなどを拒否する。

量の評価は∫B²/(2mu) dxdy [J/m]と領域別エネルギー、全Jz電流[A]、指定Ht積分[A]、固定Az反力[A]、元Hの境界周回積分[A]、元Bの外向き磁束[Wb/m]を含む。固定Az反力は元H周回積分の負に対応するが、離散反力と元場積分は別々に保存する。

離散恒等式は「反力合計+源電流−指定Ht積分=0」と「2U=源仕事+境界荷重仕事+固定Az仕事」。元Hの全周積分−源電流は別のAmpere診断量であり、離散反力が正確でもゼロを強制しない。divB=0は元Bの全周磁束から検査し、正規化には∫|B|dsを使う。境界群ごとの正味磁束は物理的にゼロになり得るため、その絶対値合計では正規化しない。残差/恒等式を元場の離散化誤差の証拠にしない。

新規6unitが12.033秒でPASS。独立解析は二層32・凹形/回転二次製造解32・全周Az=0の矩形電流24、計88例。二層は接線H連続、muによるBの変化、領域エネルギー・固定反力・左右の磁束を解析照合する。固定Az差でmuを倍率変更するとBは不変、H/U/反力は逆比例する。矩形ではJzを固定し、Az/B/Uはmu比例、Hは不変。幾何尺度sではAz∝s²、B/H∝s、U∝s⁴を別に検査する。

矩形のAzと導関数は分離変数のFourier級数、エネルギーは積分済みn^-5級数から独立に導出した。独立4×4 Gauss/Duffy求積で元Az/B/HのL2誤差を測り、2048/4096項の打切り差をFEM誤差と別判定する。P1の32/64/128、P2の16/32/80でAz/B/H/U/元H反力をそれぞれ単調改善・個別許容差で判定する。参照の尺度則は解析評価の再利用だけに使い、FEMを補正しない。打切り差は標本診断であり厳密誤差上界ではない。

独立記録は `out/planar-magnetostatic-solve-independent-20260913`、標準1172件は `out/validation-planar-magnetostatic-solve-candidate-20260913` へ終了0。固定後の候補ソースは不変。

有限の宣言境界での線形磁静場である。軸対称、曲線/穴、非線形BH、永久磁石、異方性、巻線インダクタンス、厳密開放境界、保存/CLI/GUIは別工程。新規依存・外部資料・旧版実行なし。S02と全計画は未完。

独立88例は1006.232秒でPASS。二層/製造解の最大相対差2.591356e-14、gauge/剛体変換3.252607e-15、尺度則2.333467e-12、境界磁束7.750007e-16。
最細P1のAz/B/H/U/元H反力の最大相対差は 2.202422e-04, 1.482747e-02, 1.482747e-02, 2.198538e-04, 1.556396e-02。
最細P2のAz/B/H/U/元H反力の最大相対差は 3.122313e-06, 3.407226e-04, 3.407226e-04, 1.161557e-07, 2.371238e-04。
2048/4096項のAz/B/H打切り差最大は 9.296917e-13, 3.530901e-09, 3.530901e-09。
標準2291.794秒、1169合格・任意NGSolve参照2件/HTTP環境1件skip、ResourceWarningなし。主6unitは12.002秒でPASS。715sourceと主721source（不変egg-info 6件）が完全一致。独立88例は候補hashに結び付け、本体で同じ全例を再実行したとは扱わない。旧ベンチマーク・許容差不変。統合証拠は標準出力内seed_regression.json。
