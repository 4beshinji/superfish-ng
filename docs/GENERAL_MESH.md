# G02: 一般輪郭の適合メッシュ生成

## 仕様と受入範囲

G01の単一・軸接続・直線多角形を対象とする。全頂点と各辺のaxis/PEC/対称タグを
保持し、自己交差の修復、頂点の間引き、狭い隙間の閉鎖はしない。
旧profile系のJSONとメッシュ生成結果を保持する。穴はP03、曲線要素はG03。

生成は初期分割、サイズ制御と品質改善、完成メッシュ検証の段階に分ける。
初期分割は凸な三頂点を取り出し、他の残存頂点を内部にも辺上にも含まない場合だけ
三角形として確定する。これにより直線上のタグ切替点も削除しない。
座標は最大寸法で正規化し、正の向きが数値的に解決できない場合は、対象形状の
特徴寸法確認または検証済み外部meshを案内して失敗する。端面全体からタグを推測しない。

候補三角形は面積/辺長二乗和が最大のものを優先し、同点は輪郭順とする。
これは初期分割の選択規則であり、最小角・最大辺長の保証ではない。
細分化の共有辺は単一の中点を使い、境界子辺は親辺のタグを引き継ぐ設計とする。
全域サイズと境界/角近傍サイズの具体的な入力契約、品質改善方式と停止制限は
測定後に確定する。初期分割だけで自動solveを公開しない。

G02全体の受入には次を必要とする。

- 全境界線分の完全被覆・タグ一致、正面積、接続円板位相、面積と回転体体積。
- 狭い隙間、共線タグ切替、細長い領域について、品質分布・サイズ達成・失敗条件。
- 同じ入力からの再現性、旧JSON/旧計算の回帰、生成時間・要素数・使用メモリ。
- 非円筒の独立参照との周波数・場・RF・表面量の個別収束。小残差だけでは受入しない。
- Case/CLI/保存/GUI経路の生成設定と結果の保持。

## 初期分割の実装・証拠 — 2026-09-08

`contour_mesh.triangulate_contour` は境界頂点だけの N−2 三角形を生成し、既存の
厳密なmesh入力検証を通す。`make_mesh` の一般輪郭自動生成はまだ未対応のまま。
`tests/test_contour_mesh.py` は独立矩形分解の面積/体積、円錐、幅1e-5の経路、
共線の軸/端面頂点と混在タグ、寸法1e-6〜1e6、同一入力の再現性を検証する。
狭い隙間の分割成功を、その三角形のFEM精度や品質保証とは扱わない。
標準検証は `out/validation-g02-initial-20260908/validation.json` がPASS。
標準182件中180合格・2 skip。GUIと外部参照の検証はこの段階では未実施。

## 依存判断（暫定）

この初期分割は向きと包含の計算から独立実装し、既存NumPyのみを使う。
新規依存は採用しない。以下の細分化測定だけでは品質改善・精度を受入できず、Gmsh等の
採用/不採用の最終ADRにはまだならない。G02.Sは進行中、Iは部分実装、Vは未受入。

## タグを保持する細分化と品質測定 — 2026-09-08

内部API `refine_contour(case, mesh, max_edge_m, *, max_triangles=250000,
max_passes=30)` を追加。全辺の最大長を指定し、Caseの既存boundary_max_edge_mと
corner_max_edge_m/corner_radius_mで追加の局所細分を行う。
境界指定は軸を除く全境界、角指定は輪郭の実際の方向転換点（軸端も含む）を中心とする
閉球と交わる全辺が対象。共線タグ切替点は保持するが、角の中心とは扱わない。
サイズ比較の相対余裕は1e-12。正の有限サイズ・正整数の停止上限以外は拒否する。
Case/JSONへの全域サイズ契約と自動solve公開はまだ未完了。

共有辺の中点を一度だけ生成し、短辺分割時には所有要素の最長辺も分割する。
子境界辺には親タグを継承し、毎段階で既存の完全被覆・面積・接続検証を通す。
新規要素数は生成前に上限と比較する。失敗時に途中のメッシュを完成結果として返さない。
既存profile用細分の分割規則を内部再利用した独立の経路で、旧計算経路は変更していない。

`contour_mesh_quality` は最小角、最小/中央値の q=4√3 A/Σ辺長²、最大辺長と
頂点/要素数を返す。これらは幾何測定であり、RF誤差の推定値ではない。
直角二等辺三角形の45度・q=√3/2で測定式を独立検証。
矩形分解の面積/体積、局所サイズ達成、混在タグ長、再現性、停止条件もテストした。

再現コマンド: `OPENBLAS_NUM_THREADS=1 python scripts/measure_contour_mesh.py --out out/mesh-g02-new`
（インストール済み環境で実行し、未使用の出力先を指定）。
証拠 `out/mesh-g02-refinement-20260908/measurements.json` の合成折返し領域:

| 経路幅 m | 全域最大辺 m | 要素数 | 最小角 deg | 生成秒（tracemalloc有効） |
|---|---|---|---|---|
| 0.5 | 0.25 / 0.125 | 472 / 1888 | 14.036 / 14.036 | 0.146 / 0.494 |
| 0.01 | 0.25 / 0.125 | 473 / 1827 | 0.286477 / 0.286477 | 0.110 / 0.454 |
| 0.00001 | 0.25 / 0.125 | 473 / 1827 | 0.000286479 / 0.000286479 | 0.119 / 0.454 |

tracemallocピークは約0.58〜3.02 MBで、プロセスRSS/全native確保量ではない。
サイズ達成だけでは狭い経路の悪い角が改善しないことを確認した。次は品質改善方式と
必要要素数・失敗条件を測定し、方式/依存判断を確定する。G02の品質受入は未達。
標準 `out/validation-g02-refinement-20260908/validation.json` はPASS、
185件中183合格・2 skip。非円筒RF参照・製品自動生成の受入は引き続き未実施。

## 内部対角辺の組替え — 2026-09-08

`improve_contour_angles(case, mesh, max_sweeps=100)` を追加。内部の隣接2要素について
新しい2三角形の向きが正で、最小角が厳密に改善するときだけ対角辺を交換する。
新対角辺は旧辺より長くせず、角近傍のサイズ制御も検査する。頂点・境界辺・タグは不変。
辺番号順で、1巡内では同じ要素を再編集しない。規定巡回数後にさらに改善候補が残れば
明示失敗し、途中状態を完成結果として返さない。局所改善でありDelaunay保証ではない。
共線/非凸/数値的に不確かな交換は採用しない。

検証は境界/タグと解析幾何モーメント、最大辺長と局所サイズ、最小角の非悪化、
品質中央値の改善、再現性・再適用の固定点。標準186件中184合格・2 skip、
`out/validation-g02-flips-20260908/validation.json` PASS。旧2形状の全mode量/hashは不変。

`scripts/measure_contour_mesh.py` に組替え後の品質と所要時間を追加。
証拠 `out/mesh-g02-flips-20260908/measurements.json` では最大辺0.25 mの場合、
経路幅0.5/0.01/1e-5 mの品質中央値はそれぞれ
0.7111→0.8660 / 0.03462→0.10363 / 0.00003464→0.00010392へ改善。
ただし最小角は全例で細分化後から不変。固定頂点の対角辺交換だけでは狭い経路の
角品質不足を解決できない。次は頂点配置/追加方式と必要要素数を評価する。
製品メッシュ方式・品質受入・依存判断は引き続き未確定。

## 内部頂点の移動 — 2026-09-08

`smooth_contour_interior(case, mesh, max_edge_m, sweeps=5)` を追加。
境界頂点を固定し、内部頂点を隣接頂点の平均位置へ移動する候補を試す。
移動距離の1、1/2、1/4、1/8倍を順に試し、関係する全三角形の最小qが厳密に
改善するときだけ採用する。正の向き、全域最大辺長、角近傍サイズを保持する。
境界辺は動かないため境界サイズ・タグも保持される。巡回数は最適化の予算であり、
収束保証ではない。qの改善を個々の角の単調改善とは扱わない。
入力が全域サイズを満たさない場合は先に細分するよう明示拒否する。

テストは固定境界/タグ/接続、独立面積/回転体体積、正面積、局所サイズ、
最小qの改善と決定的再現性を検査。標準187件中185合格・2 skip、
`out/validation-g02-smoothing-20260908/validation.json` はPASS。

測定スクリプトへ移動後の品質を追加。証拠は
`out/mesh-g02-smoothing-20260908/measurements.json`。全域最大辺0.25 mの場合、
経路幅0.5 mの最小角は14.036→15.609度、幅0.01 mは0.286477→0.368325度、
幅1e-5 mは0.000286479→0.000368330度。内部点の移動は改善に寄与するが、
境界点を固定したままでは狭い経路の品質不足は解決しない。
次は境界点の追加を含む配置方式、必要要素数と品質拒否条件を評価する。
G02の製品公開・品質受入・独立非円筒RF検証は引き続き未完了。

## 点追加と実測品質による生成判定 — 2026-09-08

`quality_contour_mesh(case, max_edge_m, min_angle_deg=10., max_triangles=250000,
max_rounds=12)` を内部APIとして追加。初期分割→サイズ細分→内部辺交換→内部点移動の後、
全要素の最小角を実測する。目標未達の要素の最長辺へ中点を追加し、最長辺閉包で適合性を
保つ。境界辺も分割対象としタグを子辺へ継承する。局所サイズを再検査・細分して反復。
新しい点の追加により、固定境界点で残っていた狭い経路の品質改善が可能になった。

返すのは指定最小角を全要素で満たしたメッシュだけ。角指定は有限の0度超60度未満。
反復上限では達成角と目標を示して失敗し、要素数上限でも途中メッシュは返さない。
収束や任意の角目標の実現を保証する方式ではない。10度は内部APIの幾何品質目標であり、
数値精度や表面場収束を保証する閾値ではない。製品既定値は数値受入後に確定する。

再現: `OPENBLAS_NUM_THREADS=1 python scripts/measure_contour_quality.py --out out/quality-new`。
証拠 `out/mesh-g02-quality-20260908/quality.json`。全域最大辺0.25 m、目標10度、
最大4000要素・8回の点追加で測定した合成折返し領域:

| 経路幅 m | 結果 | 要素数 | 最小角 deg | 秒 |
|---|---|---|---|---|
| 0.5 | PASS | 472 | 15.6088 | 0.173 |
| 0.05 | PASS | 831 | 12.6135 | 0.922 |
| 0.01 | PASS | 2336 | 10.0309 | 3.669 |
| 1e-5 | FAIL（要素上限） | 完成出力なし | 未達 | 1.514 |

測定スクリプトの正常終了は全ケース合格を意味しない。各行のPASS/FAILを読む。
細い経路の拒否を全一般輪郭の対応完了へ読み替えない。
テストは狭い経路の品質/サイズ達成、境界点増加、独立矩形分解の面積/体積と
品質未達/要素上限による拒否を検証。標準188件中186合格・2 skip、
`out/validation-g02-quality-20260908/validation.json` はPASS。
次は独立非円筒RF参照との収束、必要資源と方式判断、Case/保存/CLI/GUI入力契約を進める。

## 非円筒の独立照合 — 2026-09-08

[円錐台と折返し輪郭の独立RF照合](CONTOUR_RF_COMPARISON.md)を追加。
Netgen/P3 Hphiの別メッシュ・別組立・別RF積分と両側3段階で比較。
円錐台は初回PASS、折返しは初回の周波数未収束FAILを保持し、追加細分でPASS。
周波数/RQ/Gと内部電磁場の個別差・細分変化を記録した。角の表面ピークは未受入。
最細自前P2は36万自由度・約84秒であり、精度達成と計算費用を併記する。
標準189件中187合格・2 skip、独立参照専用7件PASS。
次はこの品質/時間/精度の証拠から方式判断を記録し、製品サイズ/品質/停止上限の契約と
Case・保存・CLI・GUI経路を接続する。G02全体は未受入。

## Caseの明示制御契約 — 2026-09-08

v3 contour Caseの `mesh.contour_mesh` を追加。Pythonでは
`mesh_controls.ContourMeshControls` を `Case.contour_mesh` へ渡す。

```json
{"max_edge_m": 0.003, "min_angle_deg": 10.0, "max_triangles": 250000, "max_rounds": 12}
```

max_edge_mは必須の正の有限SI長。残り3項目は上記の既定値で、省略時も正規化出力には
全値を明記する。角度は0度超60度未満、上限は正整数でboolを拒否する。
未知項目・null・非有限・profile形状への指定・v1/v2指定を拒否する。
既存nr/nz/triangulationは旧profileメッシュ契約のまま保持し、この専用設定へ変換しない。
全域サイズは明示値からだけ決め、境界/角近傍の既存サイズ指定を追加制約とする。

Case往復と生成メッシュを使うP2保存/再読込で4設定の保持を検証。
設定未指定の一般輪郭と既存の外部メッシュ経路は従来の契約を維持する。
この段階ではmake_mesh自動経路・GUI編集欄への接続は未実装で、一般輪郭の
自動solveはまだ明示拒否する。設定が存在するだけで公開済みとは扱わない。
標準191件中189合格・2 skip、`out/validation-g02-controls-20260908/validation.json` PASS。

## 自動solve・CLI・GUI接続 — 2026-09-08

make_meshはcontour_meshがある場合だけ品質判定付き生成へ進む。未指定なら必要な設定名と
外部メッシュ経路を案内する。CLIの既存 `--mesh` は生成を置換する従来の明示指定のまま。
Case設定だけの自動solveと明示生成メッシュのsolveが頂点・接続・周波数で一致し、
保存Caseの再計算で係数まで一致することをテストした。
実CLIは `examples/contour_folded.json` → `out/cli-g02-auto-20260908`、その保存Caseから
`out/cli-g02-auto-reload-20260908` へ再実行し、係数・全RF・Case hashが完全一致。
例は粗い操作用の合成形状であり、独立比較の収束済みメッシュとは別。

GUIに最大辺長mm・角度・要素数・反復数の欄を追加し、読込設定を復元/収集する。
未編集のSI最大辺長を保持し、一般輪郭で使わないnr/nz/分割方式は無効化する。
JS構文と標準191件中189合格・2 skipはPASS（validation-g02-auto-20260908）。
GUIの実ブラウザーによる編集・solve・保存往復は次の検証段階。G02全体は未受入。

## G02.S/I/V受入 — 2026-09-08

以下の要件と証拠を照合し、G01の軸接続・単一の直線外周に対するG02を受入。
有限資源で任意形状の生成成功を保証するものではなく、品質未達を明示拒否する契約を含む。

| 要件 | 実装・証拠 | 判定 |
|---|---|---|
| 方式、依存判断、測定 | ADR-017、品質4形状と独立RF/時間測定。追加製品依存なし | PASS |
| 一般輪郭の適合分割とタグ | triangulate_contour/refine_contour、元線分の完全被覆・局所タグ継承、厳密mesh検証 | PASS |
| 狭い経路、局所サイズ | 幅0.01の最小角達成、極細経路の上限FAIL。境界/角サイズを独立検査 | PASS |
| 品質判定と停止 | 全要素の最小角、生成前の要素上限、反復未達の明示拒否 | PASS |
| 面積/体積/接続/再現性 | 矩形分解・円錐・単一円板位相・同一入力再生成のtests/test_contour_mesh.py | PASS |
| 非円筒の独立参照 | CONTOUR_RF_COMPARISON.md。双方収束と周波数/RQ/G/内部H/E差 | PASS |
| 旧JSON/既存RF継続 | 旧2形状の全mode量/Case hash不変、標準192件中190合格・2 skip | PASS |
| Case/CLI/保存再実行 | ContourMeshControls、CLI新規/保存Case再計算の係数・RF完全一致 | PASS |
| GUI読込/編集/自動計算/結果/出力 | 実Chromeの5操作、gui-g02-auto-browser-fixed-20260908/report.json | PASS |
| メッシュ細分比較 | contour_mesh.max_edge_mを倍率で除算。実要素数増加と形状不変を検証 | PASS |

初回GUIは保存結果表示がprofile頂点列を参照して失敗した。一般輪郭の長さで修正し、
プローブ・掃引画面も点検した。バンド解析は適用外として明示拒否する。
再実行は5操作PASS、外部リクエスト0、実行中ソース変更なし。画面の折返し輪郭も確認。
今回のGUIサーバーは停止済み。
最終標準証拠は `out/validation-g02-gui-fixed-20260908/validation.json`。

曲線幾何/曲線要素はG03、特異角の表面ピークはN03、穴/内導体はP03、外部形式のGUI指定はO02。
これらを本受入へ含めない。32互換項目・33親課題の目標は未完で、次はG03.Sを進める。

## G03での内点候補追加 — 2026-09-08

曲線弦輪郭での品質停滞に対し、低品質内点の平均方向以外も探索するよう拡張。
境界固定・正面積・最大辺長・角近傍サイズ・局所最小品質の厳密改善を維持。
独立矩形扇で面積と回転体体積を検証し、楕円の最小角10度未達を解消した。
[曲線幾何の実測記録](CONIC_GEOMETRY.md)を参照。
探索の予算と最小角の受入条件は別であり、任意形状の成功は保証しない。
