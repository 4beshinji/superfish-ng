# 実曲線TE調整のCLI・GUI

開始HEAD `4a579e9`、2026-09-15 JST。D02の円柱部＋円弧端部のTE磁気対称z_min鏡映例を、
多項式調整版5と指数式調整版7のCLI/GUIへ通す。
受入条件は保存2試行からの再開、実Eφによる同定、元対称セクター順位の表示、
最終細分の目標差/粗細差の別判定、元場の保持、APIとCLI/GUIの全保存配列・RF一致である。

入力と比較元は[座標別境界修正時の実曲線検証](CURVE_COORDINATE_BOUNDS.md)を再利用する。
固定RF、同じ1e-14 mの接合許容差、同じ曲線・初期メッシュとuniformレベルを使用する。
独立な体積増分の証拠も同じ入力・同じ計算実装に対する前段の結果を使い、再計算を増やしていない。
目標差と粗細差はそれぞれ1 MHzの操作要求であり、離散化誤差の上限や連続枝の証明とは扱わない。

## CLI

`resume-tune`で両版のcheckpoint-002から新出力へ再開した。
各1回の最終細分だけを追加計算してTUNED。粗細差43,511.077178 Hzで両判定true。
元2試行のhash、APIと同じ要求/判定を保持する。
APIの最終場との2組16 NPZ配列および全RF量が完全一致し、元試行を含む104ファイルの保持を確認した。
記録は`out/te-cap-cli-20260915/`。追加2半領域固有値計算であり、元API計算の再実行ではない。

## GUI

既存の`verify_gui_curved_harmonic_tuning.mjs`は最終場の検査で
`curved_refinement_steps`が常に存在する前提だった。
今回のuniformレベル指定ではそのプロパティがないため、旧参照式のTypeErrorを計算なしで再現した。
検証器を履歴配列とレベル指定の両形式へ対応させ、refinement_scaleに対応する追加回数を検査する。
製品の入力・FEM・GUIコードの変更はない。
実Chromeで多項式版14項目、指数式版11項目がPASS。
フォームの要求完全一致、重複キーのworker起動前拒否、2試行で停止、厳密な保存JSON再生、
改変された試行の拒否と既存状態保持、再開後の両ゲート、最終場のID/TE非該当量を確認した。
多項式版では変更中の入力から古い準備結果を採用しないことと、最終uniformレベルも検査する。
指数式は元ASTを多項式へ変換せず保存・復元する。両版の対称セクター順位表示と結果画像を確認した。
記録は`out/te-cap-gui-20260915/`。各版2＋1の計3固有値計算、GUI合計6半領域分。
両版4ジョブはcomplete、両ブラウザー検証は終了0。ソースhash一致と外部HTTP要求0を確認。
追加の読み取り診断は、待機中に両結果がTUNEDであることを確認した。
最後の補助診断は検証完了により閉じたブラウザーへ接続できず終了したが、本検証の両report/終了0は回収済みである。

APIとのGUI全3試行×2版、CLI最終試行×2版をまとめて照合した。
8組64 NPZ配列と全RF量が完全一致し、重複を除いた182 nativeファイルのhash保持を確認した。
今回の新規計算はCLI2＋GUI6=8半領域分。比較元API6解は再利用で、新規計算数に加えない。
両版とも粗細差43,511.077178 Hz、要求・判定はAPIと同じ。すべて実FEMであり解析式による周波数の代用はない。
GUIサーバーは対象PIDだけをSIGINTで停止し終了0、全検証handleも終端回収済み。

変更はブラウザー検証器と記録のみ。製品コード、数値閾値、依存は変更していない。
前段のfull/独立体積検証を再利用し、全unittest・full/seed validatorは再実行していない。hosted CIの実行も主張しない。
[集約結果](../benchmarks/tuning/te-cap-workflows-20260915.json)を保存した。
実曲線の逐次/適応Studyの専用GUI/CLI、分割切替TE対称条件、他物理/D03などは残る。
