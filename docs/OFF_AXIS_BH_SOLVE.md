# S04: 軸非接続P1の境界付き非線形磁静場

2026-09-13 JST。固定823sourceを主829sourceへ統合し、専用API範囲で限定受入。

[限定計画](OFF_AXIS_BH_SOLVE_PLAN.md)の専用Caseと実接線Newtonを実装した。厳密r>0の材料表/来歴、全Jphi、固定psi[Wb]/接線Ht[A/m]、P1、求積q、Newton設定と明示初期相対係数を保持する。少なくとも一つの固定psiを要求し、最初の固定値に対する相対psiを解く。絶対psiとAphi=psi/r、元Br/Bz/Hr/Hzと片側材料情報を分けて出す。領域外の軸を貫く絶対磁束は補わない。

自然荷重は+2pi∫HtNi ds[A]。初期係数を指定しなければ、初期接線による固定境界の延長だけを用い、源荷重を初期場へ含めない。その後は同じ固定求積のU/g/実接線で減衰Newtonを行う。頂点までの表範囲確認、採用/棄却/失敗の全履歴を保持する。外挿・切り詰め・解析解への置換は行わない。成功は指定離散問題の残差収束であり、積分/場の精度保証ではない。

元U/U*[J]、B·Hと相対psi·gを保持し、仕事はU+U*で検査する。K@psiを内部gへ代用しない。AのH周回、+2pi∫Ht dsと比較する固定反力、Wbの2pi∫rBn dsを分ける。断面源電流と元H周回、離散反力の保存、元Bの磁束も別量にする。仕事の相殺時には、同じ差を各DOF仕事の絶対値の和で正規化する既存方式を用い、許容値1e-9は維持する。

q/q+4のK/g/電流/U/U*差と、同じ係数を高次数で評価した自由残差を保持する。元H境界積分にもq+4を使う。小さなNewton残差から求積差やメッシュ誤差の合格を推論しない。

最初の係数規約確認では、共通失敗報告がpsi_relative_to_reference_wbを受理せずValueErrorになった。版2の受理する係数名に相対psiを追加し、last_valid_coefficientsとCase内の基準を明示した。変更は受理する係数名だけでNewton演算は不変。平面版1と軸接続版2を維持し、既存成功96/失敗32 nativeを54.725秒で再読込して576ファイルと全結果/履歴の不変を確認した。元の共通モジュールと失敗ログ/判断はout/off-axis-bh-solve-development-20260913/に保持した。

追加6unitは初回25.377秒でPASS。厳密psi=-Czの1/r径方向場と径方向材料界面、対数によるU/U*、基準変更、両境界の非線形電流、線形極限、4/8/16細分、求積次数差と高次数残差、反復/範囲/初期場失敗、零B/Hと非零Aphi=C/r、厳密入力/probeを確認した。初期実装の型名から生成したDOF座標メンバー名は、物理確認前の読取りで既存のdof_pointsへ修正した。物理演算・許容差の失敗修正はない。

独立参照は、H(B)の逆関数と表折点ごとに分割した径方向積分、矩形/穴の対数によるエネルギー、独立Gauss積分の元場差を使用する。一般のpsi/Aphi誤差は、固定基準分を除いた解析場の代表尺度と全体積で正規化する。零場のAphi=C/rに限り、異なる半径評価の丸めに対する32eps|C|/rmin√Vの既存絶対許容を使用し、他の零量は厳密に保つ。これは最初の全比較より前に固定した条件である。

径方向の別BVPはF=psi-reference、F_r=rB(H)、H_r=-Jphiを既存SciPy solve_bvpで解く。両端psiまたは外側HをCaseから指定し、解析/FEM係数を初期値へ使わない。初期の自然境界比較は21節点・4反復、257点の元4種類の場で最大1.605e-16。ODE残差1e-10/境界1e-11/場差1e-8を使用する。既存[solve_bvp公式仕様](https://docs.scipy.org/doc/scipy/reference/generated/scipy.integrate.solve_bvp.html)の接口を再利用し、追加外部資料・新規依存はない。

固定823sourceの独立96条件はout/off-axis-bh-solve-independent-trial-20260913へ918.420秒でPASS（成功88/期待失敗8）。標準1305件はout/validation-off-axis-bh-solve-candidate-20260913へ終了0。固定候補は不変。保存/CLIは[後続工程](OFF_AXIS_BH_NATIVE_PLAN.md)。旧版実行なし。合成材料のみ。対象版モデルとS04、全計画は未完。

独立比較は径方向1/r解析場24、二材料細分24、折点電流細分24、旧線形8、零場8、期待失敗8。最大解析差2.931e-14、係数尺度/基準変更1.689e-14、旧線形差8.882e-16。60の試行棄却も実行した。全16細分系列の元psi/Aphi/B/HはP1 8/16/32で改善し、開始許容値を満たした。独立BVP全8比較もPASS。各量の数値は独立report.jsonに保持する。

全16細分系列の最細最大誤差はpsi 2.045e-4、Aphi 2.154e-4、B 0.009964、H 0.012578、U 3.685e-4、U* 3.933e-4、元H周回0.019319、元反力0.013350、磁束8.705e-4。反力は2pi×周長×解析H尺度、周回は周長×H尺度と源電流、磁束は2pi R²×B尺度で正規化した。各量の開始許容値は不変。別BVP 8比較の257点最大差は1.640e-15。

標準2716.280秒、1302合格・任意NGSolve参照2件/HTTP環境1件skip、ResourceWarningなし。主18unitは48.651秒でPASS。標準/独立の固定823sourceと主829source（不変egg-info 6件）は完全一致。独立全例を主で再実行したとは扱わない。旧ベンチマーク・許容差不変。統合証拠は標準出力内seed_regression.json。
