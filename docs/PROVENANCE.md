# 独立実装・情報来歴

## G03法線オフセット区間と要件照合 — 2026-09-08

normal_offsets.normal_offset_bounds / partition_normal_offsetを追加。
円錐曲線フィレットの中心軌跡C=x+dNとC'=(1−dκ)x'を有理数区間で囲む。
二進回転係数のc²+s²を保持し、符号付き曲率/速度に反映する。
sin/cos中点級数+Lipschitz、sinh両端、coshと速度の有理数平方根境界を使用。
円の定曲率では相関を使い、半径分の内側オフセットが中心へ潰れることを保持する。
正則FORWARD/REVERSED、円のCOLLAPSED、ゼロを含むUNVERIFIEDを区別する。
全有限fraction区間を被覆し、予算不足/分離不足で未処理区間を除外しない。
PASSは全区間の正則性だけで、交点・尖点の存在/根数・フィレット候補完成ではない。

追加6テストは円の半径変化/潰れ、楕円/両枝双曲線頂点、点/数値微分の包含、
既知楕円折返し位置の保持と全区間被覆、予算/厳密入力、方向/尺度、
Decimalによる二進回転頂点の独立照合。未実装ImportErrorを先に確認した。
双曲線頂点の中心方向の初期テスト期待値は法線符号から訂正し、閾値緩和なし。
変更前363件中361合格・2 skip、最終369件中367合格・2 skip（94.587秒）。
out/validation-g03-normal-offsets-20260908 PASS。seed_comparison.jsonでcase hash一致、
周波数差0、RF/エネルギー差最大8.882e-16。FEM/数値基準の変更なし。
検証中の実装変更なし。GUI/Wineの新規実行なし。

同所offset_partitions.jsonは要求曲線/controls/区間結果を有理数表記で保存。
円はSINGULAR（1区間）、楕円は39区間処理後に18正則・2未確認を保持してUNVERIFIED、
双曲線は全区間正則でPASS。楕円の未確認を失敗根除外や尖点数証明へ読み替えない。
この基盤は通常Case/GUIからの弧フィレット構築へまだ接続していない。

G03_ACCEPTANCE.mdを新設し、CONIC_GEOMETRYの5段階と計画G03の要件を照合した。
曲線FEM/幾何別細分の既存comparison.jsonのPASSと、前回Chrome8操作のpassed=trueを
現在のファイルで再確認。全曲線入力互換・弧を含むフィレット・物理ピーク収束は不足を明示。
最小半径のプリミティブ値と、任意輪郭への制約契約も区別する。
README・対応表・計画・実装状況・バックログを同期した。

新規外部資料/コード/依存/旧資産参照なし。初等微分幾何と既存有理数区間核から独立導出。
次は中心軌跡の交点探索、有限接点/有向切詰め、保存/CLI/GUI、独立形状/FEM検証。
8親課題の限定受入を維持し、G03全体・全計画目標は未完了。


## G03版4指定半径の線分間フィレット — 2026-09-08

line_filletと構築schema_version=4を追加。2本の有向線分の支持交点を二進入力から
有理数で求め、R tan(|θ|/2)の切詰め距離で小円弧を構築する。交点以降の生成幾何は
浮動小数点を含み、支持直線距離・有限範囲・G1・保持方向を数値検査する。
版2/3の区間認証とは区別し、GUIにも指定半径・円弧長・数値検査と表示する。
有限線分の延長は明示bool、空/逆向き保持線分・平行/反転・数値未達を拒否し、
半径を自動縮小しない。線分/円弧/線分を完成Case全体の検査へ通す。

既知直角接点/中心/1/R曲率/除去面積を先にテスト化し、未実装ImportErrorを確認。
追加7テストは回転/鏡映/尺度/逆順、浅い/鈍い転向、過大半径/延長/厳密指定、
保存改変拒否/GUI共通APIと解析面積、二次曲線FEM相似則までPASS。
初期テストのNumPy scalar入力をbuilt-in floatへ修正し、数値未達用fixtureは
端点がfloatで一致する直角から非直角へ訂正した。製品の型契約・許容差は緩和していない。
変更前標準356件中354合格・2 skip、最終363件中361合格・2 skip（94.553秒）。
out/validation-g03-line-fillet-20260908 PASS。seed_comparison.jsonでcase hash一致、
周波数差0、RF/エネルギー相対差最大8.882e-16。FEM本体/数値基準の変更なし。

out/gui-line-fillet-browser-20260908/report.jsonのChrome8操作PASS、外部要求0、
実行中ソース変更なし。tangent-construction.pngの半径/長さ/検査範囲表示を確認。
明示選択・保存ダウンロード・再構築・適用・曲線FEM/描画・改変拒否まで実操作済み。
検証用GUIサーバーは停止済み。validation内construction_replay.jsonには
以前の版1/2/3と今回の版4の実保存ファイル再読込をCASE_VALIDATEDで記録した。

例はexamples/construction/corner_fillet_request.json。合成長方形断面の右上角を
R=0.02 mで丸め、geometry_order=2 / element_order=2を明示。
実CLI構築/Case/solveはout/g03-line-fillet-construction-20260908.json、
out/g03-line-fillet-case-20260908.json、out/g03-line-fillet-solve-20260908。
周波数1152045668.073337 Hz、R/Q(acc)=57.172456 ohm。GUIと同じ値を確認。
幾何の解析不変量とFEMの相似則の証拠であり、この形状のRF/ピーク収束は未検証。

式は初等直線/円幾何から独立導出。新規外部資料/コード/依存/旧資産参照なし。
Wine新規計算なし。次は円/楕円/双曲線弧を含むフィレットとG03全要件照合。
線分間の部分受入で全フィレット対応とは扱わず、8親課題の限定受入を維持する。
G03全体・全互換目標は未完了。


## G03版3固定支持直線・有限弧の構築統合 — 2026-09-08

line_arc_tangentと構築schema_version=3を追加。固定支持直線の接線条件w²=nQnと
接点c+Qn/wを二進入力から厳密有理数で求める。非接線・無限遠接点を区別し、
有限線分の延長可否と元の順序/保持端点を明示する。枝/有限弧/fractionを区間判定し、
空線分/空弧・逆向き・未確認は接続しない。生成端点と保持弧外端の位置誤差を囲む。
G1角度は数値検査。近傍線の自動補正・任意フィレット・角度区間認証とは区別する。
要求/保存/再構築・Case/CLI/GUIへ接続し、版1/2の既存保存文書も実再読込できた。

追加7テストは既知円/双曲線接点、枝/端点/延長/方向、非接線/漸近線、予算/厳密controls、
保存/GUI共通API、合成カプセルの解析面積/体積と実FEM相似則。
標準356件中354合格・2 skip（92.557秒）、out/validation-g03-line-arc-20260908 PASS。
同所seed_comparison.jsonでcase hash一致、周波数差0、RF/エネルギー相対差最大8.882e-16。
数値基準/FEM本体/許容差の変更なし。

out/gui-line-arc-browser-20260908/report.jsonのChrome8操作PASS、外部要求0、
実行中ソース変更なし。tangent-construction.pngで保持端/接点と誤差上界の表示を確認。
保存ダウンロード・再構築・明示適用・FEM/描画・改変拒否まで確認し、GUIサーバーは停止済み。
実CLI記録はout/g03-line-arc-construction-20260908.json、out/g03-line-arc-case-20260908.json、
out/g03-line-arc-solve-20260908。例はexamples/construction/capsule_line_arc_request.json。
版1のout/gui-tangent-browser-final-20260908/downloads/tangent-construction.json、
版2のout/g03-certified-capsule-construction-20260908.jsonもCASE_VALIDATEDで再読込した。

版2/3カプセル比較はvalidation内capsule_v2_v3_comparison.json。
双方617節点/1152三角形で、f相対差2.32491e-8、両RQ差0.626214%、G差8.12673e-8、
TTF差0.310662%。微小な構築差から再生成したメッシュ同士の比較であり、純幾何誤差と
FEM/RF誤差を分離していない。このカプセルのRF精度収束は未検証として保持する。
接点誤差上界をRF精度保証に読み替えず、差を隠す補正も行わない。

式は既存二次形式と初等幾何から独立導出。新規外部資料/コード/依存/旧資産参照なし。
Wine新規計算なし。次はフィレットとG03全要件照合。8親課題の限定受入を維持し、
G03全体・全互換目標の完了は宣言しない。


## G03版2区間付き構築・保存・CLI/GUI統合 — 2026-09-08

certified_constructionと構築schema_version=2を追加。確定fraction区間内のfloat位置を選び、
生成弧の数学的端点と出力floatを囲むboxから、厳密接点への距離上界を検査する。
外側の保持端点も誤差上界を確認する。G1角度は生成プリミティブの数値検査として区別。
第1弧END/第2弧STARTは元弧を再利用し、空弧・零長・逆向き・未達は候補と理由を残して選択不可。
版1の従来経路と保存文書の実再読込を維持。版2は同じCLI/GUIで保存/再構築/Case/FEMまで接続した。

最終標準349件中347合格・2 skip（91.637秒）、標準数値回帰PASS。
out/validation-g03-certified-construction-20260908のseed_comparison.jsonでcase hash一致、
周波数差ゼロ、RF/エネルギー相対差最大8.882e-16。基準更新なし。
Chrome8操作はout/gui-certified-construction-browser-20260908/report.jsonでPASS。
外部要求0・実行中ソース変更なし、tangent-construction.pngの誤差上界表示を確認。
検証用GUIサーバーは停止済み。Wine新規計算なし。

追加5テストは区間内切詰め/誤差上界、端点再利用/空弧拒否、版2保存/版1継続、
不正指定/位置未達、GUI応答と合成カプセルの解析面積/体積・実FEM相似則。
例題はexamples/construction/capsule_certified_request.json。
実CLIの構築/Case/solveはout/g03-certified-capsule-construction-20260908.json、
out/g03-certified-capsule-case-20260908.json、out/g03-certified-capsule-solve-20260908。

版1/2カプセル比較はcapsule_v1_v2_comparison.json。617節点/1152三角形は同数だが
メッシュ座標/接続は異なり、f差1.41259e-8、RQ差0.312004%、TTF差0.154291%を記録した。
接点位置誤差の保証をRF精度保証にせず、この形状のRF収束は未検証。補正/閾値変更なし。
仕様・再現・初期版との区別はTANGENT_CONSTRUCTION.mdとGUI_ACCEPTANCE.md。
新規外部資料/コード/依存/旧資産参照なし。

次は直線と弧の接線/フィレットとG03全要件照合。G03全体と全互換目標は未完了。


## G03有限弧所属とfractionの有理数区間 — 2026-09-08

certified_arcs.certified_finite_arc_tangentsを追加。支持曲線の接点boxから有限弧の
INTERIOR/EXTERIOR/START/END/UNVERIFIEDを判定し、fractionを有理数の閉区間で返す。
楕円は2 rad以下のsectorの和集合、双曲線は枝とsinhの単調性を使う。
端点sin/cosはTaylorのLagrange剰余、sinhは正項級数の幾何残尾で囲む。
float atan/asinhや経験的epsilonは判定根拠にしない。厳密な一点一致だけをSTART/ENDにする。
fractionはprefix所属の二分で囲み、未分離/予算不足では最後の有効区間を保持する。

最終標準344件中342合格・2 skip（86.157秒）、標準数値回帰PASS。
out/validation-g03-certified-arcs-20260908のseed_comparison.jsonでcase hash一致、
周波数差ゼロ、RF/エネルギー相対差最大8.882e-16。基準更新なし。GUI/Wineは今回未実行。

対象は保存された二進パラメータと支持係数。元の実数入力の丸め前まで認証しない。
独立8検査で10^-20 rad端点近傍、周期/逆向き/内部sector境界、双曲線枝/端点、
Decimal.expによる独立sinh照合、既知fractionの1/2・1/3・2/3、各予算不足を確認。
元の合成カプセル候補1件も両点INTERIORとfraction幅2^-32でPASS。
有理数レポートはout/validation-g03-certified-arcs-20260908/capsule_membership.json。
仕様と式は[TANGENT_CONSTRUCTION.md](TANGENT_CONSTRUCTION.md)。新規外部コード/資料/依存/旧資産参照なし。

次は区間から選んだ切詰め位置の接点誤差上界と明示位置許容差の検査、空弧/端点再利用の操作契約、
版付き構築・保存・CLI/GUIへの統合。既存の版1構築/GUIは数値ガード判定のまま保持する。
直線と弧の接線・フィレット・G03全要件照合は継続。全互換目標は未完了。


## G03支持曲線の接点座標区間 — 2026-09-08

contact_enclosures.supporting_contact_enclosuresを追加した。既存の孤立法線根区間から、
接触定数・接点座標を有理数区間で伝播する。厳密根は一点として扱い、D=0の±sqrt分岐を保持する。
分母の0分離に失敗すれば未確認。box幅上限の未達でも生成boxと候補番号を保存する。
返却float接点から厳密接点までの距離上界も別に返す。

最終標準336件中334合格・2 skip（85.740秒）、標準数値回帰PASS。
out/validation-g03-contact-enclosures-20260908に検証ログとseed_comparison.jsonを保存。
seed case hash一致、周波数差ゼロ、RF/エネルギー相対差最大8.882e-16。基準更新なし。

局所座標の厳密逆行列から双曲線指定枝を区間符号で検査する。
二進cos/sinのc²+s²を1と仮定しない。回転0.37 radの有理単位円点(3/5,4/5)の
順逆変換が一点boxで厳密一致する追加検査はlocal_inverse_invariant.jsonに保存した。
支持曲線の二進係数に対する区間であり、元の実数回転・測定寸法や有限弧端の所属を認証しない。
既存の有限弧構築・保存形式・CLI/GUI判定にはまだ接続せず、別APIとして公開する。

独立8検査は既知円/楕円/双曲線の無理数接点の二乗比較、根幅→box幅減少、
直角回転/移動/2^-20〜2^20尺度、返却接点誤差上界、指定枝反転、幅未達・未完根・一致曲線・不正幅。
仕様と再現は[TANGENT_CONSTRUCTION.md](TANGENT_CONSTRUCTION.md)。
新規外部資料/コード/依存/旧資産の参照なし。接触式と既存有理数境界から独立実装した。

次は局所接点boxと有限弧端の比較、内外/端点判定と区間に基づく切詰めの版付き構築統合。
双曲線の有限区間・楕円の有向周期区間、直線と弧の接線/フィレット、G03全要件照合は残る。
全互換目標は未完了。GUIは前回受入のままで今回新規実行しない。


## G03接線構築GUI受入 — 2026-09-08

GUIへ構築要求/保存結果の読込、接点・長さ・方向の候補表、明示選択、閉輪郭検査、
構築保存、編集画面への明示適用を追加。tangent_documentはCLIと同じ構築/再構築/Case経路を使う。
要求の編集・候補変更・別ファイル読込で古い結果を解除し、非同期の古い応答も除外する。
読み込み/候補表示だけでは現在のプロジェクトを変更しない。適用時は要求の計算条件も反映する。

最終標準328件中326合格・2 skip（85.344秒）、標準数値回帰PASS。
out/validation-g03-tangent-gui-final-20260908のseed_comparison.jsonでcase hash一致、
周波数差ゼロ、RF/エネルギー相対差最大8.882e-16。ベンチマーク更新なし。
out/gui-tangent-browser-final-20260908のChrome7操作PASS、外部要求0、実行中ソース変更なし。
保存再読込→適用→FEM→場表示、改変拒否まで実操作し、tangent-construction.pngを表示確認した。
GUI合成カプセルf=1170098360.3437803 Hz。物理精度収束や旧版照合の証明ではない。

初回out/gui-tangent-browser-20260908はJavaScript JSON再生成で1.0等の表記が変わり、
保存後の厳密再構築照合がFAIL。サーバーが生成したserialized文字列をそのまま保存する修正後に再検証した。
初回数値回帰はPASSだったがGUI未受入であり、最終版とは区別する。失敗ログは保持。
再現と操作はGUI_GUIDE.md、GUI_ACCEPTANCE.md、TANGENT_CONSTRUCTION.md。
検証用GUIサーバーは停止済み。新規外部資料/コード/依存/旧資産参照なし。

次は接点区間/弧端認証、直線と弧の接線・フィレットとG03全要件照合。
GUIの追加をG03全体の完了や全互換目標の完了とは扱わない。


## G03接線構築の保存・Case・CLI — 2026-09-08

tangent_construction.pyに版1の構築要求/結果と保存後の再構築照合を追加した。
case_templateは未完成の編集文書、候補未選択のcaseはnullと明示する。
連続する2つのPEC弧を明示選択で弧/線/弧へ置換し、既存Caseで閉輪郭と全設定を検査する。
construct-tangent/export-constructed-caseを追加し、既存solveまで接続。
primitiveのstrict読書きを既存CurvedContourと共通化。計算式と依存は変更していない。

最終標準326件中324合格・2 skip（86.428秒）、標準数値回帰PASS。
out/validation-g03-construction-cli-20260908にtests.log/validation.json/seed_comparison.jsonを保存。
seedのcase hash一致、周波数差ゼロ、RF/エネルギー相対差最大8.882e-16。基準更新なし。
検証実行中のsrc/tests/scripts/examples変更なし。GUI/Wineの新規実行なし。

合成カプセルの解析面積/回転体積、実FEMの寸法2倍でf半減・両RQ/G/TTF不変、
保存再構築/改変検出・未確認出力・不正設定/上書き拒否など追加7検査を実装した。
初回例題は長さ0.2 mから0.6 mへ変えた際のjoin_toleranceを流用して境界padding検査に失敗。
入力の許容差を1e-12 mと明示し、既存の許容差上限や判定は変更しなかった。
詳細と再現コマンドは[TANGENT_CONSTRUCTION.md](TANGENT_CONSTRUCTION.md)。

例題はexamples/construction/capsule_request.json。実CLIの構築/書出/solveは
out/g03-capsule-construction-20260908.json、out/g03-capsule-case-20260908.json、
out/g03-capsule-solve-20260908。基本モード1170.098360 MHz。
これは合成例の動作/相似則検証であり、この形状の物理精度収束・旧版照合ではない。

次はGUIの候補表示/明示選択/保存/Case適用。厳密接点区間/弧端認証、直線と弧の接線、
フィレット、G03全要件照合は継続。全計画目標は未完了。
新規外部資料/コード/依存/旧資産の参照なし。


## G03有限弧数値判定と明示選択G1接続 — 2026-09-08

arc_tangents.pyへ有限有向弧の区間/枝判定、方向記録、明示選択の切詰め・局所G1接続APIを追加。
既存の支持曲線列挙を保持し、候補を勝手に選ばず、元曲線の中心/半軸/回転/枝を変えない。
弧端はガード付き未確認。fractionは数値推定であり、厳密な接点区間包囲ではない。
仕様・独立9検査と初回期待値の訂正理由は[TANGENT_CONSTRUCTION.md](TANGENT_CONSTRUCTION.md)。

開始時310件中308合格・2 skip。追加後の独立全テストは、実行中にmodule docstringを
編集してStudyのソース変更検査1件が作動した。数値実装の変更ではないが失敗を保持する。
標準validate内の最終全319件中317合格・2 skip（85.826秒）、標準数値回帰PASS。
出力はout/validation-g03-finite-arcs-20260908、初回失敗はinitial_parallel_tests_source_changed.log。
seed_comparison.jsonで円筒6/成形セル3モードのcase hash一致、周波数差ゼロ、
RF/エネルギー相対差最大8.882e-16。ベンチマークは変更していない。GUI/Wineの新規実行なし。

次は構築要求/選択結果の保存・Case/CLI/GUI・閉輪郭検証への接続。
厳密な接点区間/弧端の認証、直線と弧の接線、フィレット、G03全要件照合は残る。
全33親課題の継続目標は未完。新規外部資料/コード/依存/旧資産の参照なし。


## 主要文書の現状同期 — 2026-09-08

製品基準e68002fのソースと個別受入記録に基づき、README・現状表・互換対応表/計画・
バックログ・曲線要素の入口・物理仕様の適用範囲を同期した。
8親課題の限定受入とG03の部分受入を区別し、過去の未接続/実行中記録は履歴と明示。
最新の接線候補核を有限弧の構築完成へ読み替えず、物理ピーク収束の未検証を保持した。
新規外部資料・コード・依存・旧資産の参照なし。製品/テスト/ベンチマークの変更なし。
標準310件中308合格・2 skipを再確認。文書のみのためvalidate・ブラウザー・Wineは再実行しない。


## 曲線写像の逆評価 — 2026-09-08

既存二次写像の多項式係数からNewtonと凸包細分を独立実装。
多項式の既知写像と球形の独立物理点を検証に使用。
新規外部資料/コード/依存なし。未確定を領域外の成功判定に変換しない。

## 曲線壁と軸RF — 2026-09-08

二次幾何の接線ノルムを使う壁積分を独立実装し、直線軸は既存振動多項式積分へ接続。
独立円柱壁積分と球形参照で検証。新規外部資料/コード/依存なし。
表面ピークの精度を積分量の一致から推論しない。

## 曲線解とエネルギー正規化 — 2026-09-08

既存の一般化固有値/エネルギー規約を曲線空間へ明示適用。
物理勾配で場を求め、既存の独立球形参照と体積積分で照合した。
新規外部資料/コード/依存なし。振幅を参照解へフィットしない。

## 全体曲線空間の検証 — 2026-09-08

既存共有メッシュから全辺の幾何/位相検査を集約し、局所行列を共有番号へ組立て。
球形の独立参照で周波数と積分次数を照合。
R29の全域Jacobianの背景を既存実装後に確認。コード/図は転記せず、新規依存なし。

## 二次境界の独立検査 — 2026-09-08

二次Lagrange節点からBezier制御点へ代数変換し、凸包とde Casteljau二分で離隔を検査。
解析的に交点が分かる放物線を独立反例として使用。新規外部資料/コード/依存なし。

## 曲線区間と共有幾何節点 — 2026-09-08

既存の等fraction弦分割から区間を保持し、メッシュ辺の投影位置から独立に曲線節点を生成。
Jacobian二次多項式はBernstein凸包下界を先行使用。全体境界証明とは区別する。
独立解析モーメントと反転例で検証。新規外部資料/コード/依存なし。

## 二次幾何写像の独立実装 — 2026-09-08

既存P2基底の座標補間と微分、二次多項式の極値から独立実装。
新しい式は公開資料や旧コードから転記せず、局所写像とTM弱形式から導出。
独立な多項式曲線領域のモーメントで検証。新規外部資料/コード/依存なし。

## 球形の独立参照 — 2026-09-08

NIST DLMFの球ベッセル定義/微分式（R28）を参照。
PEC条件・SI peakエネルギー正規化・RF積分は本プロジェクトで独立導出した。
旧コード/数値を使わず、FEMと別の関数/積分で照合。新規依存なし。

## 双曲線閉領域の検証 — 2026-09-08

公開デッキを流用せず、明示した双曲線式から合成閉領域を定義。
zによる面積/体積積分、鏡映とスケール不変量でnative経路を検証。
直角の座標入替えを幾何評価へ共通適用。新規外部資料/コード/依存なし。

## 内点の方向探索 — 2026-09-08

近傍平均で停滞する矩形扇から独立に移動候補不足を再現し、8方向の有限探索を追加。
正向き/サイズ/局所品質改善の既存条件を保持。楕円の品質停滞と細分を再検証。
新規外部資料/コード/依存なし。角度/数値受入閾値は変更していない。

## 明示サイズ内の対角線交換 — 2026-09-08

旧対角線長の単調減少制約を、明示された最大辺長の制約へ一般化。
独立四角形の角度と面積/回転体体積で検証。楕円の残る品質FAILも保存。
新規外部資料/コード/依存なし。閾値緩和は行っていない。

## G03の細分検証 — 2026-09-08

元楕円/FEMを独立に固定する既存Studyの再現スクリプトを追加。
メッシュ品質FAILとFEM細分PASSを分離し、反復上限増でも残る停滞を保存。
独立電磁場参照値とは主張しない。新規外部資料/コード/依存なし。

## G03の解析曲線GUI — 2026-09-08

既存のnative曲線と弦輪郭をGUIへ接続。元曲線を保持し解析面積/体積との差を表示。
実ブラウザーで編集・計算・保存再読込を確認。新規外部資料/コード/依存なし。

## G03のStudy分離 — 2026-09-08

元曲線とFEM制御を固定した弦誤差条件列を既存Studyへ接続し、楕円体の解析体積で検証。
粗い3段階は数値判定FAILのまま保存。経路テスト成功を数値収束へ読み替えない。
新規外部資料/コード/依存なし。幾何細分とFEM細分を別入力として維持する。

## G03の解析曲線鏡映 — 2026-09-08

鏡映行列と曲線のパラメータ変換から元曲線を独立変換し、既存FEM係数鏡映へ接続。
楕円半領域で幾何/エネルギー2倍と全領域の別固有値solveを照合した。
数値接続許容値は緩めず、同値な三角関数表現を選択。新規外部資料/コード/依存なし。

## G03の解析体積 — 2026-09-08

Greenの定理による体積境界積分と円錐曲線の指数表現から独立実装。
円柱/円錐台/楕円体と、双曲線のzによる別積分を独立不変量に使用した。
弦体積との差を保存し、曲線FEMの精度と混同しない。新規外部資料/コード/依存なし。

## G03の元曲線保存 — 2026-09-08

native曲線パラメータをv3へ保存し、導出弦Contourとの一致を検査する。
現行の直線FEMと解析幾何を明示区別。保存係数の再現を検証し、未移行の鏡映は拒否する。
新規外部資料/コード/依存なし。曲線要素の実装済みとは扱わない。

## G03の弦Contour変換 — 2026-09-08

端点差を明示的な多角形調整として記録し、その変位を弦誤差予算へ加算する独立実装。
解析曲線は変更せず、元曲線対応と面積差を保持する。半楕円/回転楕円体の解析幾何で検証。
新規外部資料/コード/依存なし。直線への変換を曲線FEMの完成とは扱わない。

## G03の曲線閉輪郭 — 2026-09-08

自前プリミティブ・接続・区間包絡検査を統合し、解析線積分から正面積を確認する。
半楕円面積と矩形/軸鎖を独立不変量に使用。未確認辺対や大きい隙間を受入へ読み替えない。
新規外部資料/コード/依存なし。解析曲線の製品入出力・曲線FEMは未完。

## G03の隣接曲線分離 — 2026-09-08

有向位置微分の極値と共通投影の単調性から端点近傍を分離し、残りの区間対を包絡箱で検査。
中心差分と既知接続/重なりで検証。数値的に判定できない接続を受理せずUNVERIFIEDにする。
新規外部資料/コード/依存なし。閉輪郭全体と曲線FEMはまだ未完。

## G03の区間包絡 — 2026-09-08

回転曲線の座標微分零点から包絡箱を独立構成し、箱間距離と二分探索で離隔を検査。
倍精度の余裕付き判定を形式的な区間演算保証とは呼ばない。
既知の極値/距離と交差・未確認ケースを独立検証。新規外部資料/コード/依存なし。

## G03の接続検査 — 2026-09-08

線分の直接式と有向単位接線のatan2から接続検査を独立実装。
主軸端接線・矩形面積・逆向き/位置差の独立条件で検証した。
接線一致を曲率連続や閉輪郭の安全性と同一視しない。新規外部資料/コード/依存なし。

## G03の双曲線弧 — 2026-09-08

cosh/sinhのnative有限区間表現を微分・積分して独立実装。
陰関数/勾配直交と頂点曲率半径を独立不変量に使用し、パラメータ反転・弦誤差も検査。
新規外部資料/コード/依存なし。旧NT=3の全指定との互換や曲線FEMは未完。

## G03の楕円弧 — 2026-09-08

楕円のパラメトリック式を微分/積分して接線・曲率・面積寄与を独立実装。
二階微分上限と線形補間のGreen核から弦誤差上限を導出した。
陰関数/勾配直交・円極限・四分楕円面積を独立不変量として検査。
新規外部資料/コード/依存なし。旧形式の楕円/双曲線パラメータ対応や曲線FEMは未完。

## G02受入と実操作修正 — 2026-09-08

実Chromeで一般輪郭の結果表示に残るprofile前提を検出し修正。細分Studyも
無効なnr/nz変更だけで終わらず、全域最大辺長を細分するよう修正した。
幾何不変量・独立RF参照・失敗条件・GUI往復をG02要件へ対応付けて受入。
新規外部資料/コード/製品依存なし。曲線/穴/特異ピークや旧版全仕様の完了とは扱わない。

## G02の自動生成接続 — 2026-09-08

品質判定付き自前生成をCase設定から呼び、既存FEM/保存経路へ接続。
明示メッシュとの一致と保存Caseの再計算を検証。合成操作例は精度基準と区別する。
新規外部資料/コード/依存なし。GUIは実装/構文検証段階で、実操作は未検証。

## G02の方式選択と制御契約 — 2026-09-08

既存の品質・精度・時間測定からADR-017で自前生成を第一方式とした。
全形状の成功や速度優位は主張しない。制御値を明示した型とv3入出力へ保持する。
新規外部資料/コード/依存なし。設定の保存を自動solve/GUI接続の完了と扱わない。

## G02の独立RF照合 — 2026-09-08

既存の公開NGSolve参照の幾何入力を単一全PEC輪郭へ拡張し、Maxwellからの内部E式を追加。
自前生成メッシュを参照へ渡さず、別メッシュ/P3 Hphi/別積分と比較した。
初回の折返し未収束FAILを残し、双方をさらに細分して同じ閾値でPASSを確認。
新規外部ソース参照・コードコピーなし。既存ADR-011の検証専用依存を隔離環境へ再導入。
完全独立の線形代数、領域全場誤差、角部ピークの保証は主張しない。

## G02の点追加品質判定 — 2026-09-08

自前の共有辺中点分割を共通内部関数へ抽出し、品質未達要素の最長辺選択と反復を追加。
境界点追加を許しタグを保持する。目標達成の実測だけを成功条件にし、一般収束は主張しない。
独立矩形モーメントを検証し、極細経路の要素上限FAILも保存。新規外部資料/コード/依存なし。

## G02の内部頂点移動 — 2026-09-08

隣接頂点平均への有限候補移動と正面積/最小q比較から独立実装。
境界と局所サイズを保持し、面積/体積の独立不変量を検証した。
新規外部資料/コード/依存なし。改善量と狭い経路に残る品質不足を区別して記録。

## G02の内部辺組替え — 2026-09-08

隣接三角形の向きとatan2による角比較から内部対角辺交換を独立実装。
既存境界/頂点は動かさず、厳密な局所最小角改善を条件とした。
追加外部資料/コード/依存なし。狭い経路の最小角未改善という測定結果を記録し、
一般メッシュ品質の完成とは扱わない。

## G02の適合細分化 — 2026-09-08

既存mesh.pyの共有中点/最長辺閉包の分割規則を一般輪郭専用経路へ内部再利用。
端面推測を使わず親辺タグを継承する。面積/回転体体積・解析三角形品質を独立検証。
測定では狭い経路の最小角が改善しないため、サイズ達成を品質受入へ読み替えない。
新規外部資料/コード/依存なし。測定結果と限界はGENERAL_MESH.mdに記録。

## G02の初期分割 — 2026-09-08

向き・三角形包含・辺の接続から全頂点を保つ分割を独立実装。
矩形分解と円錐の解析モーメントを独立不変量として検証した。
新規外部資料/コード/依存なし。品質保証・依存方式の最終判断はGENERAL_MESH.mdの
残件に留め、初期分割の成功をFEM精度保証へ読み替えない。

## G01受入判断 — 2026-09-08

独立幾何モーメント・変分境界条件・全領域FEM・GUI/保存往復を対応付けて受入。
外部メッシュによる計算可能性を自動生成や数値精度保証へ読み替えない。
追加外部資料/コード/依存なし。

## G01の径方向ギャップ — 2026-09-08

独立の矩形分割から真空外区間を特定し、probe/plotのNaNとinsideを検査。
領域外を補間で埋めず、一般輪郭へ不適切な解析ラベルを付けない。
新規資料/外部コード/依存・旧内部参照なし。

## G01の局所境界検証 — 2026-09-08

端面全体の境界要約と局所辺条件を分離。拘束空間の包含による変分固有値順序を
独立不変量として採用し、解析値への補正なしで検査。新規外部資料/コード/依存なし。

## G01 GUI往復検証 — 2026-09-08

実ローカルChromeで閉輪郭とタグの完全保持を確認。画面確認で見つけた
端面欄の無効な編集操作を修正した。外部資料/コード/依存の追加なし。

## G01の輪郭プレビュー — 2026-09-08

閉輪郭と開いたR(z)を明示区別して既存GUIへ渡す。形状の自動修復や半径への置換なし。
新規外部資料/コード/依存・旧内部参照なし。

## G01の鏡映FEM検証 — 2026-09-08

一般輪郭の幾何鏡映を既存P2係数鏡映へ接続。
独立した全領域FEMを同じメッシュで再計算し、振幅補正なしで比較。
新規資料/外部コード/依存・旧内部参照なし。

## G01の外部mesh境界照合 — 2026-09-08

既存ディスク位相検証へ一般輪郭の線分投影被覆を追加。
テストメッシュは矩形の明示分割から独立作成し、一般メッシュ生成の完成とは扱わない。
新規外部資料/コード/依存・旧内部参照なし。

## G01のCase契約 — 2026-09-08

一般多角形をv3の別geometryとして保持し、旧R(z)入力へ変換しない。
未移行のメッシュ経路を明示拒否。新規外部資料/コード/依存・旧内部参照なし。

## G01のprofile変換・鏡映 — 2026-09-08

既存境界を軸/端面で閉じ、鏡映後の有向辺を単一ループとして接続する独立実装。
解析円錐台体積と面積/体積不変量で検査。新規外部資料/コード/依存・旧内部参照なし。

## G01の単一多角形コア — 2026-09-08

線分の向き/交差判定と境界の解析モーメントから独立実装。
矩形分割と円錐体積を独立不変量として検査。新規文献/外部コード/依存・旧内部参照なし。
合成z折返し例を実測構造と呼ばない。

## N02受入判断 — 2026-09-08

高次場の各経路と独立証拠をHIGH_ORDER_FIELDS.mdに対応付け、直線P2を受入。
周波数だけでRF/表面精度を代用せず、円筒の結果を特異角・曲線要素・適応へ一般化しない。
追加の外部資料/コード/依存はない。

## N02の総合場/ピーク証拠 — 2026-09-08

円筒場を既存解析式のBessel/三角関数と独立に評価し、元FEM場の誤差を検査。
表面最大値はJ1の導関数零点とPEC場から導出。新規外部資料/コード/依存なし。
同DOF比較は周波数・E/H・軸・RQを分離し、小残差を空間精度の証拠にしない。

## N02のGUI次数 — 2026-09-08

既存Case契約をGUIの次数選択へ接続し、別の計算器を作らず通常Jobへ渡す。
実ローカルChromeで計算・保存場表示・Project往復を検証。新規依存/外部コードなし。

## N02の次数入力契約 — 2026-09-08

既存v3 strict schemaへ次数を追加し、実P2組立の選択と保存再現へ接続。
次数を物理種別と区別する。新規資料/コード/依存・旧内部参照なし。

## N02のバンド・細分比較 — 2026-09-08

二次軸辺の内部停留点を含めた零点数診断と、軸場差二乗のGauss積分を導入。
既存の場重なりと棄却条件を維持する。新規外部資料/コード/依存・旧内部参照なし。

## N02の保存P2解析比較 — 2026-09-08

既存の円筒Bessel参照と高次保存係数を結び、軸場誤差を元要素内で積分する。
解析式をFEMへ補正として使わない。新規資料/外部コード/依存・旧内部参照なし。

## N02のP2描画接続 — 2026-09-08

保存済み係数の検証済みreaderと既存の高次標本化を描画へ接続した。
図のために別の固有値計算や解析補正を使わない。新規外部コード/依存なし。

## N02の高次保存検証 — 2026-09-08

元メッシュからP2空間を再構成し、保存した座標/接続/軸/境界と厳密照合する。
独立の故障注入でmanifestだけでは防げない構造不一致を検査。
新規外部資料/コード/依存・旧内部参照なし。

## N02の高次表示データ — 2026-09-08

P2辺中点による4分割と元の高次場の標本化を独立実装。
表示用補間をRF計算へ流用せず、VTKタイトルで表示分割を明示する。
新規外部資料/コード/依存・旧内部参照なし。

## N02の高次鏡映 — 2026-09-08

三角形の向き反転に伴う局所辺順を独立に導出し、全中点の偶奇へ適用した。
振幅を調整せず、再組立残差・場の偶奇・U/損失・別の全領域FEMで検証。
新規外部資料/コード/依存、旧内部参照はない。

## N02のPEC表面量 — 2026-09-08

二次uとアフィン辺の積からHの三次式、Maxwell微分からEの二次式を構成。
既存NumPy多項式演算で内部停留点を探索する。7次Gauss積分の独立照合は
既知多項式とQUADPACK、RF収束は既存の円筒解析式で行った。
外部コード/新規依存/旧内部参照なし。角部の推定値に誤差上界を付与していない。

## N02の二次軸電圧 — 2026-09-08

二次多項式のLegendre展開から指数関数との積分を構成し、既存SciPyの
球Bessel関数で安定評価する。原始関数と実根から絶対値積分を独立構成。
QUADPACKの重み付き数値積分を独立照合に使用。新規外部資料/コード/依存、
旧内部の参照なし。FEM解への解析補正は行わない。

## N02のP2任意点場評価 — 2026-09-08

N01のLagrange多項式を点評価し、その微分から既存Maxwell場を独立復元する。
二次多項式の解析微分と軸極限を独立検査。外部コード/新規依存/旧内部参照なし。
RFや表示への製品統合は未完で、HIGH_ORDER_FIELDS.mdに範囲を記録した。

## N01の二次Lagrange TM要素 — 2026-09-08

既存のu=Hphi/r弱形式へ二次Lagrange基底を代入し、K/Mを独立実装した。
単項式の階乗積分を組立の独立不変量とし、Bessel固有値で収束を検証した。
解析式をFEM解の代用や補正に使わず、新規外部資料・コード・依存・旧実行形式の参照はない。
P1組立の積分経路は維持。P2研究APIとP1後処理の混用を拒否する設計判断はQUADRATIC_ELEMENTS.md。

## R01の加速量規約 — 2026-09-08

区分線形場の部分区間積分と複素位相回転を、既存の積分式から独立に拡張した。
円筒参照はcos場×指数関数の閉形式と|cos|原始関数から別実装し、SciPy quadratureで検査。
FEM場を解析値で補正/代替していない。新規外部資料・コード・依存、旧ソルバー参照/実行なし。
保存・CLI・GUI・半領域鏡映に同じ規約を渡し、既定結果とhashが旧値に一致することを確認。
初期の対角線差、区間外掃引、スクロール待ち失敗をACCELERATING_CONVENTIONSへ記録した。
旧入力変換の適用範囲や科学的ゲートは緩めていない。

## O01の完了公開と旧結果移行 — 2026-09-08

同一ファイルシステムの非置換hard linkと完了manifestを使い、直接保存とJob取込の
完了時点を独立実装した。新規外部コード・文献・依存・旧ソルバー参照はない。
保存前後の数値不変性、故障注入・SIGKILL・競合を検査し、元からあるC02の計算結果を
再読して旧形式互換を確認した。新形式のJob取込も外部meshと完了証拠を保持する。
旧結果のテストfixtureは実際に旧形式のmetadata/ファイル集合へ戻して検証した。
受入範囲・永続性の限界・実測はSAVE_COMPLETION.md。科学的な許容差の変更なし。

## C02の限定AFパーサー — 2026-09-08

前日にLEGACY_INPUT.mdを仕様化し、REG/PO字句解析・位置保持・単位換算・strict拒否を独立実装した。
根拠はC00で記録したR25 VI.1/2の入力インターフェースと保存済みAF。旧内部の参照なし。
既存合成AF2形状を読み込み、NGを4回新規計算して既存SFOの周波数・RF量と照合した。
旧ソルバーの追加実行、ソース/実行形式の閲覧、新規依存・外部コード流用はない。
合成unittestの入力は新規に作成。実際の旧入力バイトと出力はignored out/だけに保存する。
物理・FEM/RFの数式と許容差は変更せず、独立寸法/面積/体積/Bessel検査と直前基準差ゼロを確認した。

## C01の明示物理契約と移行 — 2026-09-07

MODEL_CONTRACT.mdに基づきv3 case、Model、能力表、明示移行を独立実装した。
既存PHYSICS.mdの真空TM縮約・単位・境界を保持し、未実装の物理と材料を拒否する。
Project/Study/GUIの版/モデル保持と、版移行をまたぐ同一物理比較を修正した。
新規外部文献・ソース・依存の利用、旧計算実行や旧内部参照はない。
初回失敗と修正後の8テスト、標準validate、Chrome操作を実行し、受入をMODEL_CONTRACTへ記録。
既存円筒/非円筒の全RF辞書とcase hashは直前検証に一致。基準・許容差の変更なし。

## C00の版付き入力台帳 — 2026-09-07

ユーザー指定の互換開発goalに着手。既存ローカルR25の抽出テキストのVI章で、
REG/PO文法、変数・単位、曲線指定、材料入力の記載を確認した。
抽出時の表の欠落・空リンク・文字化けは仕様確定の根拠にしない。
保存済み合成necked cellの入力・設定・数値出力を再読し、4ツール各自の表示版を記録した。
COMPATIBILITY_BASELINE.mdとcompatibility_reference_inventory.jsonに節・hash・不明点を保持する。
旧ソース・実行形式内部の閲覧、追加旧実行、外部コードのコピー・新規依存追加なし。
文書・生の旧入力/出力は同梱しない。hashと書誌的情報だけを台帳に収録する。
K02/K12の文書証拠をDと分類し、文書記載と実行互換を区別した。
製品コード・FEM・物理モデル・許容差・既存ベンチマークは変更していない。

## 互換対応表と作業分割 — 2026-09-07

実態整理後、ユーザー指示に従いCOMPATIBILITY_MATRIXとCOMPATIBILITY_PLANを作成した。
基準は既存NGコード/受入記録と、R26の公式配布機関の機能概要、R27のUSPAS講義の概要。
歴史的な機能一覧を対象7.xに移し替えず、L/H/U/Xの証拠区分とC00の仕様確認作業を置いた。
インストーラー名7.20と既存SFO表示7.17の違いはSUPERFISH_COMPARISON.mdから確認した。
検索に非公式ミラーが表示されたがリンク先を開いていない。旧ソース/バイナリの取得・閲覧・実行なし。
参照したのは公開の機能概要で、ソルバーの内部手法を新規実装へ転用していない。
材料/平面RF/永久磁石/力・トルク等を計画へ追加したが、製品の物理仕様・入力受理範囲は変更していない。
計画表の新課題は未着手。数値基準・過去の失敗/受入記録を変更していない。
文書検査で32対応項目、33個の一意な親課題、対応先/依存IDと30ローカルリンクを確認した。
git diff --check合格。製品・テストのファイル変更なし。

## 実装計画の実態整理 — 2026-09-07

ユーザー指定の順序で、コード・テストと受入記録を照合して
IMPLEMENTATION_STATUS、BACKLOG、ROADMAPを更新した。PMC端面/GUIの完了、
一般追跡と原子的保存の残件を区別し、旧依存関係を訂正した。
製品コード・物理仕様・既存ゲート・ベンチマークを変更していない。
基準unittestを再実行。旧ソース/バイナリの閲覧・実行なし。

## 非構造メッシュ入力 — 2026-09-07

既存Mesh/Case/FEM/RFと公開された三角形の接続・面積不変量から
境界タグ付きJSONの検査とCLI/API経路を新規実装した。
独立不変量は番号置換に対する周波数・RF量と円筒解析解への収束。
テストの非構造分割には既存依存SciPyのDelaunayを使用し、新規依存は追加しない。
参照メッシュ生成プログラムや旧コードのコピー・追加閲覧なし。
仕様・限界・受入結果はMESH_INPUT.md。旧入力形式互換の完成とは扱わない。

## NGSolve独立照合 — 2026-09-07

ユーザーの互換拡張指示を受けP0-01を実施。公開Maxwell式からHphi弱形式と
参照RF積分を独立実装し、NGSolve/Netgenの公開APIを使用した。
APIの参照先、試作時の軸微分誤りと修正、再現結果はINDEPENDENT_COMPARISON.md。
NGSolveの公式LICENSEと導入wheelのライセンス情報を確認。外部実装のコピーなし。
比較は今回定義した円筒と円錐台で、実機・旧コード由来入力ではない。
旧ソース/実行形式の参照、Wineの追加実行、依存バイナリの同梱は行っていない。

## 汎用GUI・共通入出力の計画 — 2026-09-06

ユーザーの目的と追加原則、既存NGコード・仕様・セミナー受入記録から
[GUI_IO_PLAN.md](GUI_IO_PLAN.md) を新規作成した。
例題への作り込み禁止、全体品質に基づく設計判断、共通入力、適用条件付きモード同定、
段階別受入を記録した。原本PDF・旧入力・旧ソルバー内部の新規参照はない。
外部コードや依存の追加、数値変更、GUI方式の決定は行っていない。

## 角部表面電場の切り分け — 2026-09-06

ユーザー依頼により、既存合成necked cellの表面片側値・固定距離プローブ・局所境界辺長を比較する
診断スクリプトを追加した。円筒Bessel解、接線と半径の幾何不変量を独立検査とし、
半径3 mmの接線フィレット形状を新たな合成対照として構成した。FEM/RF本体の変更・較正なし。
NISTIR 7668 §2.2 [R24]の一般的な角点べき則を閲覧。RFの局所Neumann主部への対応は独立に導出し、
場の補正・ピーク値の代用には使わない。外部実装コードのコピーなし。

現在の作業checkoutは `/home/sin/code/agent/reserch/superfish-ng`。過去資料の
`/home/sin/code/superfish` と過去のout/はこの環境に存在せず、新規NG計算から開始した。
その後ユーザーがWine環境の新設を明示的に許可したため、既存の一時フォルダにある
`/tmp/superfish-docs.bhOA6i/SUPERFISH/PoissonSuperfish_7.20.exe` を実行して専用Wineへ導入した。
インストーラーUIの利用許諾を確認した。実行ファイルと付属文書は再配布しない。
旧ソルバーのソース・DeveloperFiles・実行形式の内部は閲覧しない。
Wine初期化の不足DLLを実行ログで確認したが、ソルバーのアルゴリズムの追跡はしない。

管理者パスワードなしで構築するため、Ubuntu公式のWine32・Xvfbと依存パッケージを
`/tmp/superfish-wine-runtime` へ展開した。既存Wine64と同じ9.0系を使用する。
`prefix-ready` が専用Wine prefix、`wine-local.sh` が環境指定ラッパー。
失敗した初期化prefixと計算出力も保存する。実行バイナリ・ランタイムはソース配布に含めない。

比較では生成AF/SEG/IN7、ローカルSF.INI、SFO/SF7/Automeshの数値出力を読む。
SF7の斜面上でゼロになる出力は比較基準にせず、0.1/1 µmの真空側固定座標を両コードで評価する。
これは境界判定の診断であり、NGの表面値とWineの内部点を黙って比較する変更ではない。
数値と判断の範囲は [SURFACE_FIELD_DIAGNOSTICS.md](SURFACE_FIELD_DIAGNOSTICS.md) に記録する。

丸み対照の細かい折れ線をWineの粗い格子に与えた試行はError 344で失敗したため、
付属公式SFCODES.DOCのAutomesh PO入力仕様（NT/RADIUS）を確認した。
NT=4が反時計回り、NT=5が時計回りの指定であることを用い、同一の接線円弧を出力した。
参照したのは入力インターフェースの仕様で、旧ソルバーの実装コードは参照していない。
検索時に文書内の場補間の概要も表示されたが、NGの数値実装への転用は行わない。
NGの円弧近似をWineへ較正せず、元の半径・接線位置と出力境界の半径を独立に検査する [R25]。

## モード別追加参照と作業データ保存設定 — 2026-09-05

7セルπモードのWine参照側収束未達を確認し、同一形状の追加DXをモード別に検証する処理を新規実装した。
生の符号付き場でバンド全体を再同定し、元の未達結果を保持する。外部コードの参照・コピーや数値補正はしていない。
DX=0.01/0.011 cmの実行失敗について、インストール済みSF.INIの設定説明と数値実行ログを読んだ。
設定説明の `StoreTempDataInRAM=No` を新規out/内のローカルSF.INIだけに指定し、追加Wine/SF7計算を完了した。
既設SF.INI、旧ソルバーのコード・実行形式には変更を加えていない。作業データの保存方法を変える設定であり、
形状・物理条件・収束ゲートは変更しない。参照結果にはローカル設定ファイルのhashも保持する。

## 全演習一括実行 — 2026-09-05

既存のNG例題スクリプトを新規出力先へ順次実行する標準ライブラリのドライバーと、静的な入口HTMLを作成した。
全NGを新たに解き、Wine参照だけを検査して再利用する経路を明示した。未実行の照合・UI検査は合格にしない。
headless検査へ選択UIを持たない静的ページの画像・リンク検査を追加し、キーボード検査と区別して記録する。
新しい文献・外部コード・依存は使っていない。PDF4原本のSHA-256をローカル計算して対象版を記録した。

## 交差分割と円弧の鏡映 — 2026-09-05

周期セルの0/πモード等振幅と幾何反射を不変量として、四辺形の中心へ4分割する選択肢を新規実装した。
円弧の鏡映では、反射と壁巡回順の反転を合わせたindex写像を用い、半径と面積2倍を検査した。
新しい外部文献・コード・依存は使用していない。旧ソルバー内部は参照せず、NGの固有場を補正していない。
反射半領域だけの試験でR/Q収束が改善しなかった結果もout/に保持し、成功例だけに置き換えていない。

端部full-cell比較形状はユーザー提供演習1-1 p.28の図のiris中心切断から構成した。
元の周期・孔・円弧を保ち、両端に半ディスクを残す。旧ソルバーの新しいコードや実装は参照していない。
full-endのモードは実際の軸場零交差で記述し、half-end用の位相ラベルを転用していない。

## 円弧・バンド同定・ブラウザー検証 — 2026-09-05

ユーザー提供のrounded4/7 AFの端点と3 mmの円弧指定を独立JSONへ定義した。
短円弧の中心・角度・弦誤差と円弧部分の面積は一般の円幾何から実装し、独立fixtureで検査した。
旧ソルバーのソースは参照せず、既存OUTAUTの境界座標8点で半径一致を確認した。
NT指定の確認で公開資料を検索したが、CERNの該当PDF本文はbot対策画面で取得できなかった。
検索先の非公式転載や旧ソースへは進まず、参照根拠はローカル入力と数値出力に限定した。
7セルの小さいR/Qで未収束の検査結果も保存し、合否の閾値を緩めていない。

モード同定はセルのcos振幅パターン・零交差数と一対一割当て、分散は線形最小二乗で新規実装した。
Chrome公式headless/API文書（R22/R23）を参照し、Node標準機能だけで生成HTMLを検証するスクリプトを新規作成した。
外部の実装コードやサンプルのコピーは行っていない。ブラウザー本体は既設の検証ツールで、配布物へ同梱しない。

## 垂直段差の4セル演習 — 2026-09-05

ユーザー提供 `SUPERFISH/4cell_flat/flat_1.af` の寸法をSIの独立JSON入力として定義した。
段差を保つ共有半径格子・軸スラブの三角形生成を新規実装し、面積と解析積分の不変量で検査した。
旧ソルバー内部は参照していない。同じ輪郭から比較AF/SEGを生成し、WineのAUTOFISH/SF7を4モード実行した。
SF7の軸長を入力長へ一般化したが、NG数値を参照値で較正していない。初回の未収束R/Q差も記録した。
依存追加なし。設計判断はARCHITECTURE.mdのADR-005、比較状態はSEMINAR_MULTICELL.md。

## 半領域の対称境界 — 2026-09-05

既存のMaxwell弱形式から電気対称の自然条件、磁気対称のu=0、反射時の場の偶奇性を導出した。
新しい外部コード・依存ライブラリ・文献は使用していない。最初に未対応入力と半領域の周波数・損失・
反射後電圧のテストを追加して失敗を確認し、実装後に解析値・独立全領域FEMと比較した。
Wineとの比較には既に取得した全領域のSFO/SF7を入力バイト・出力hash検査の上で再利用した。
Wineの半領域モデルを実行したという主張ではない。記録はSEMINAR_PILLBOX.md。

## Pillbox演習の拡張 — 2026-09-05

TM0npの公開数学に基づく場からエネルギー・壁損失・電圧を導出し、独立積分で検査する解析参照を追加した。
計算ソルバーを補正・置換していない。可視化はNGの保存場を直接評価する。
WineのSF7を比較用に実行し、符号付きの電場出力を使用した。
実行方法は既存ExamplesのIN7とバッチの起動方法、SF.INIのSF7設定から確認した。
旧コードの内部アルゴリズム・ソースは参照していない。
SFOの絶対値表を高次モードへ適用した初回比較はFAILとして残し、SF7に基づく結果と混同しない。
実行記録と未完了項目は [SEMINAR_PILLBOX.md](SEMINAR_PILLBOX.md) を参照。

## Wine版との照合と演習マイルストーン — 2026-09-05追記

ユーザーからWineで動く本物のSUPERFISHとの照合を明示的に依頼された。
既設AUTOFISHをブラックボックスとして実行し、AF/SEGの入力規約とSFO数値出力を確認した。
旧ソルバーのソース・実行形式の内容の参照、翻訳、逆解析は行っていない。
入力はNGの幾何仕様から比較用に生成し、生の旧形式出力はGit対象外のout/に保管した。
照合に合わせてNGの導電率を揃えたが、FEM/RFの実装は変更していない。
追加のユーザー指定に基づき、ルートの演習本編、Q&A、付録1、付録2のPDFと
対応するSUPERFISH/Pillbox・4cell_flat・4cell・7cellの入力形状を参照し、演習マイルストーンを策定した。
資料中のソフトウェア・入力ファイルをOSSライセンスへ変更したものではない。
過去のseed節の「未参照・未使用」はseed作成時点の記録であり、今回の比較用実行は本節に明示する。

## ローカル開発開始 — 2026-09-05

ユーザー指定の `superfish-ng-0.1.0-seed.zip` を展開し、77エントリのCRCとmanifest対象76ファイルのSHA-256を確認した。
`/home/sin/code/superfish` 直下へ配置し、元READMEは `README-legacy.md` に保存した。
本フォルダには以前から旧環境と講義資料があるため、プロジェクトの参照・配布境界をAGENTS.mdと.gitignoreへ記録した。
旧環境の案内READMEとファイル名のみ確認し、旧ソース・実行形式・講義資料の内容は参照していない。
P0-00として、ZIP収集を明示的な許可リストに変更し、合成fixtureで配布対象外ファイルとシンボリックリンクを検証する。
FEM・RFの数式や実装は変更していない。新しい文献や外部ソルバーのコードは使用していない。
依存ライブラリはPyPIから専用.venvへ導入し、ソース配布には同梱しない。
今回の検証記録は `docs/LOCAL_DEVELOPMENT.md` を参照。

## 今回行ったこと

2026-09-05のユーザー依頼に基づくAI支援による新規実装。
ユーザーが提示した過去の議論には式・機能案・権利関係の一般論が含まれ、旧ソースコードは含まれていなかった。
実装のcanonical仕様はPHYSICS.md、検証基準は公開のMaxwell方程式とpillbox解析解から作成した。
今回のセッションで旧SUPERFISH/POISSONソース、legacy実行形式、非公開資料は取得・使用していない。
旧コード由来の入力デッキを同梱していない。2例題の寸法は今回作成した合成値。

参照した資料と閲覧範囲はREFERENCES.md。関連検索の結果に非公式SUPERFISHミラーへのリンクが表示されたが、
そのリンク先ソース・ファイルは開いていない。MFEM公式例の存在は例一覧で調べ、実装のコピーは行っていない。
cavsim2dはREADMEの製品・依存情報だけを確認し、ソルバ内部のソースを開いていない。

## 主張の限界

これは**参照情報を制限した独立実装**であって、組織的な二チーム分離を実施・監査したclean-room認証ではない。
LLMの学習データに何が含まれていたかは証明できない。別セッションに分ければ学習由来の問題が消える、という主張もしない。
著作権・特許・契約・商標・所属機関の権利を一括してclearance済みとはしていない。
この記録は技術的な来歴管理であり、法的判断書ではない。

## 今後の参照方針

公開の数学・論文・公式API・明示的なOSSライセンスのある現代の依存ライブラリを利用できる。
コードを再利用する場合は出典、バージョン、ライセンス、変更箇所を記録し、独自実装と混同しない。
legacy実装の再翻訳・decompile・同梱は実装経路に入れない。
将来、適切に利用できるlegacy出力を比較基準に使う場合は、実装と独立した参照データとして管理する。
ライセンス条件が分からない出力・入力・図表は「公開されていたから再配布可」としない。

## 新規参照データの記録テンプレート

```json
{
  "id": "reference-case-id",
  "source_url": "public URL or internal record identifier",
  "retrieved_date": "YYYY-MM-DD",
  "permission_or_license": "verified condition; do not guess",
  "solver_name": "name",
  "solver_version": "exact version",
  "geometry_sha256": "hash of an actual geometry file",
  "boundary_conditions": "all surfaces including ends",
  "materials_and_temperature": "specified values",
  "units": "SI or conversion description",
  "phasor_and_rq_convention": "explicit formula",
  "mesh_convergence": "evidence path",
  "reference_uncertainty": "value or unknown",
  "allowed_redistribution": false
}
```

このテンプレートは空の書式であり、実測・legacy参照データが存在することを意味しない。

## 初期成果物の監査可能性

- `benchmarks/validation/validation.json` は実行環境と、その実行に使ったsource/tests/scripts/examplesのSHA-256を記録。
- ZIP内 `MANIFEST.sha256` は同梱ファイルを検査するためのチェックサムであり、第三者認証や電子署名ではない。
- 初期Git履歴はユーザー環境で開始する。架空の研究者署名・貢献履歴は作らない。
- Apache-2.0のLICENSEとNOTICEを用意した。将来の人間による貢献には所属機関条件の確認を含む来歴記録を求める。

## 境界実長と局所細分 — 2026-09-06

ユーザー承認に基づきP1-04の境界メッシュ改善を実施。既存NGメッシュの辺中点分割、
共有辺の接続、長辺分割の閉包を独立実装した。新しい外部資料・コード・依存を使用していない。
旧ソルバー内部の参照やWineの追加実行はない。受入不変量は辺の物理長、領域の解析積分、
PEC接線電場ゼロ、Pillbox解析値。結果はPHYSICAL_MESH_REFINEMENT.mdに記録する。
デフォルトの変更や表面電場の平滑化は行わず、局所指定を含む入力を出力へ保存する。

## 共通プロジェクト・ローカルGUIの初期実装 — 2026-09-06

GUI_IO_PLANの開発をユーザーが承認し、同計画の受入完了をgoalに設定した。
既存Case/FEM/保存・描画を再利用し、輪郭部分の反復・接続、strictプロジェクト文書、
別プロセスの計算状態、manifest、HTMLフォームを新規実装した。
輪郭面積・全長、円弧半径と終点番号、既存入力往復、中止/保存失敗の独立検査を追加した。
ソルバー・RF数式の変更なし。参照はPython公式http.server/tkinter API文書（ADR-009）、
既存のheadless検証コードと本プロジェクトの仕様。外部実装コードのコピーや新規依存追加なし。
旧ソルバー・原本PDFの追加閲覧/実行なし。UIの最初の受入は合成円筒でありKEK実機ではない。

## 汎用操作と検証の追加 — 2026-09-06

同じ承認範囲で保存結果読込・半径プローブ・独立解析比較・条件群・GUI操作を追加した。
既存NGのFieldSampler、解析式、half-endバンド同定、FEM/RFを再利用した独立実装。
例題名は受入スクリプトのfixture選択にだけ使用し、製品の計算・表示分岐に使用しない。
3/5セルの合成輪郭はこの検証のための任意形状で、KEK/LANL実機ではない。
原本PDF・旧ソース/実行形式の追加閲覧なし。新規Wine照合は実行していない。
開発整形ツールの公式資料と使用版はDEPENDENCIES.md、設計判断はADR-010を参照。
過去のセミナー照合・表面場診断は独立した履歴として維持する。

最終受入はGUI_ACCEPTANCE.md。失敗した検証の出力を保持し、履歴順・画像保存・
SI往復丸め・非同期選択の修正を記録した。数値基準・既存ベンチマークの緩和はない。

## G03曲線入力と保存整合性 — 2026-09-08

既存のCase、O01保存完了、独立実装した曲線空間・K/M・RFを再利用して接続した。
再読込で元弦メッシュから曲線空間を再構成し、保存配列の照合と係数の物理整合性を検査する。
ソルバーを再実行して保存係数を置き換えない。VTKは既存ASCII直線表示形式を再利用し、
曲線計算用幾何と区別した。新規の外部資料/旧資産閲覧、コード転用、依存追加なし。
受入の球形は合成形状と既存独立解析式。詳細と未完経路はCURVED_ELEMENTS.mdに記録。

## G03曲線境界の問い合わせ — 2026-09-08

二次Bezier式の軸極値による単調分割と二分法を独立実装し、既存の保存プローブ/描画へ接続。
既知多項式曲線を独立不変量として使用。新規外部資料/旧資産の閲覧、コード転用、依存追加なし。
表示用細分は物理積分に使用しない。受入と残件はCURVED_ELEMENTS.md。

## G03曲線Study比較 — 2026-09-08

既存の磁場重なりと軸/RF比較を、独立実装済み曲線写像/保存解へ接続した。
物理点の重みを写像Jacobianで計算し、自己比較の一致と既存の球形解析参照で検査。
新規外部資料/旧資産閲覧、コード転用、依存追加なし。曲線再メッシュによる幾何変更を明記し、
固定幾何FEM誤差の受入に読み替えない。数値基準・基準結果の緩和なし。

## G03固定幾何の細分 — 2026-09-08

親二次多項式と参照小三角形へのアフィン写像の合成から、4分割と疎補間行列を独立実装。
既存の二次写像・辺検査・K/M組立てを再利用。GalerkinのP^T A_child P=A_parentを
同一場のエネルギー保存として検査した。新規外部資料/旧資産閲覧、転用、依存追加なし。
保存幾何の祖先と解析曲線への再投影を混同しない。通常経路接続は次段階。

## G03固定幾何細分のCase/保存接続 — 2026-09-08

既存の二次写像制限と保存再構成を共通関数で接続。初期元メッシュを保持し、Study間でも照合。
軸中点は同じアフィン写像を端点平均で計算して既存RF規約を維持する。
既存球形解析式で固定幾何細分の周波数/軸/RF比較を検証。外部資料/旧資産閲覧、
コード転用、依存追加なし。数値許容差や基準結果の緩和なし。

## G03曲線GUI入力 — 2026-09-08

既存のCase/Study/保存描画経路へHTML/JavaScriptの入力欄を接続し、既存Chrome検証を拡張。
外部通信を検出するブラウザー受入を使用。新規依存・外部コード/資料の転用なし。
旧ソルバー/資産の追加閲覧や実行なし。合成楕円のUI検証を任意形状の物理受入へ読み替えない。

## G03曲線表面場 — 2026-09-08

検証済み二次写像の境界への制限と隣接セル向きから、片側場と外向き法線を独立実装。
既存SphereTMの境界式によりE/Hの極/赤道最大と表面場を検査した。
新規外部資料/旧資産の閲覧、コード転用、依存追加なし。サンプル最大を連続極値と扱わず、
通常RFの未評価ピークを上書きしない。数値許容差・基準結果の緩和なし。

## G03連続表面極値の囲い込み — 2026-09-08

二次写像の逆Jacobianから表面電場の有理式を直接導出。Bernstein係数の凸結合と
正の分母係数による重み付き平均から商の上界を構成し、Fractionで演算した。
新規外部資料/旧資産閲覧、コード転用、外部依存追加なし。標準ライブラリFractionを使用。
保存浮動小数点入力が表す離散場の数学的上界であり、物理精度やclean-room認証とは無関係。
狭い既知有理ピークと既存独立球形解を検査に使い、通常RFの未評価状態は保持した。

## G03連続ピークの出力契約 — 2026-09-08

既存の厳密有理数上界処理を通常RFと保存再検証へ接続し、評価方式に版を付けた。
旧データは既存の積分のみの契約で読み、未評価を後から計算済みへ書き換えない。
既存の位相積分・正規化不変量と独立球形、ローカルChromeを使って検証。
新規外部資料/旧資産の閲覧、転用、依存追加なし。物理的な角と収束の未認証を明記する。

## G03解析曲線の角診断 — 2026-09-08

元曲線の端点接線と正向き輪郭の旋回角から角分類を独立実装。
平面幾何の既知270度角を不変量とし、軸/異種境界では物理的な角の判定を保留する。
新規外部資料/旧資産閲覧、コード転用、依存追加なし。離散ピークの増加を観測しても、
2段階だけで発散や物理的な最大値の収束を断定しない。旧保存版の再検証を維持する。

## 固定二次空間の鏡映 — 2026-09-08

二次Lagrange写像の座標反射と局所節点の向き反転から独立に構成した。
弱形式のr不変性とz導関数の符号反転から、偶奇部分空間のK/Mエネルギー2倍を検証する。
曲線は元Caseの解析プリミティブを用い、細分済み二次境界は再投影しない。
既存公開数式・本実装のみを使用し、新たな外部資料・旧資産の参照はない。
合成半球の4検査は鏡映変換の証拠であり、測定構造や物理ピークの認証ではない。

## 曲線鏡映の保存再構成 — 2026-09-08

独立に実装した固定写像の鏡映を通常API/CLIへ接続した。元半領域のCaseと弦メッシュから
決定的に写像を再構成し、保存された偶奇係数・全領域弱形式・RFを再検証する。
固有値再計算、係数の振幅フィット、解析式による解の代用はない。
追加の外部資料や旧資産は使用していない。合成半球の左右/両対称の検証であり、
測定構造への適合や物理ピーク収束の認証ではない。

## 合成楕円/双曲線の曲線P2追加受入 — 2026-09-08

既存の本実装の解析プリミティブと合成例を用い、固定二次幾何でのFEM細分、
双曲線の偶奇鏡映の物理不変量、通常GUIを新たに検証した。
外部参照や旧資産は追加していない。FEM比較と鏡映のPASSは、元曲線への幾何近似収束や
実機測定への一致、物理ピークの有限性/収束を意味しない。

## 曲線鏡映GUIの意味表示 — 2026-09-08

既存の保存された鏡映宣言に基づいて部分スペクトルの意味をGUIへ表示した。
合成双曲線の左右/両対称でブラウザーのチェックボックスを操作し保存結果を検査。
新規の外部資料や旧資産は使用していない。GUI表示のPASSを物理ピーク収束へ拡張しない。

## 二次幾何近似の独立モーメント照合 — 2026-09-08

既存の合成楕円・双曲線の輪郭式を直接積分した面積/回転体積を検証専用参照とし、
二次写像の体積積分と照合する。解析参照をFEM解へ代入しない。
外部資料・旧資産の追加参照はない。初回の非減少をFAILとして残し、
別の幾何近似系列で検証を継続する。モーメント誤差を局所境界誤差や物理ピーク誤差の保証としない。

## 二次幾何近似の追加系列結果 — 2026-09-08

同じ合成例と独立モーメント参照で、より細かい幾何近似を含む3段階系列を検証した。
初回の停滞/増加のFAILは保存し、受入閾値やFEM式を変更せずに追加系列のPASSを記録した。
新たな外部参照はない。幾何間のRF変化には残るFEM誤差が含まれ、積分モーメントから
局所境界誤差・物理ピーク収束の認証を推論しない。

## 接線構築の根数基盤 — 2026-09-08

R30の双対円錐曲線の接触条件を確認し、本実装の中心/Q表現から四次消去式を独自導出した。
R31のSturm列と端点/重根の扱いを参照し、有理数除算/gcd/平方因子除去/区間分離を独立に記述。
本文・図・コードの転載や旧資産の参照はない。MIT講義資料も閲覧したがSturm節が未完成で、
根数実装の根拠には使用しない。参照のURLと確認範囲はREFERENCES.md R30/R31に記録。
現実の入力誤差や元曲線の実数パラメータ精度を、二進係数多項式の厳密根数で保証したとは扱わない。

## 共通接線の四次式と接点再構成 — 2026-09-08

既存R30/R31とTANGENT_CONSTRUCTION.mdの独自消去式をもとに、Fraction係数化、
2法線座標表示、D=0処理、接点再構成とfloat返却値の再検査を独立に実装した。
円の既知接線数/距離と円錐曲線陰関数を検証参照に使用し、接線候補を定数で返してはいない。
追加の外部コード・図・旧資産は使用していない。
円錐曲線全体の候補は元の有限有向弧や指定双曲線枝にまだ制限しておらず、その状態を明示する。
