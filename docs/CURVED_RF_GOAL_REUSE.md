# RF指標の検証済み親子移送を共有する

2026-09-14追記: 共有した`_nested_transfer`内部の[共通履歴再構築](NESTED_RECONSTRUCTION_REUSE.md)も一度にまとめた。
両メッシュのstrict解析を保持し、同じ読込済み場での完全一致とRF指標約1.50〜1.53倍を追加確認した。
以下の2026-09-09計測とは変更箇所と測定を区別する。

2026-09-09。[単発のRF選択](CURVED_RF_GOAL_INDICATOR.md)では、
親子native再構築と一様制限を指標本体と追跡経路で繰り返していた。
さらに指標内で親要素番号を得るため、同じ一様制限を再実行していた。
前回の3形状の実測では指標が一様確認solveより高価だった。

`_nested_transfer`が検証した実行中の移送・空間・係数・履歴を、内部追跡関数へ共有する。
公開の`track_nested_curved_modes`は従来通り毎回native再構築と検証を行う。
既存の追跡数学・質量組立次数・正規化・割当・閾値・報告内容は変更しない。
内部の共有値をJSON/checkpointから信頼して受け取る経路は追加しない。

RF指標は一様1段の確認をすでに検証しているため、その4子要素の順序から親要素番号を復元する。
幾何や係数を再計算して比較する重複処理を除いた。
個々の残差作用、随伴、RF規約、選択配列は維持する。

初回の切り出しでは報告用のmapping変数が未定義で検査が失敗した。
この経路の明示値nested_curvedへ修正し、初回ログを保持した。
修正後のRF選択4検査（1.652秒）・公開親子追跡5検査（7.609秒）がPASS。

`scripts/validate_curved_rf_goal_reuse.py`は既存3形状のnative親/確認を読み、
同じ読込済み入力に対する新旧計算結果の完全一致を各3回検査する。
最初はsolve直後に作った保存指標との完全一致を要求したが、変更前コード自身が不一致となった。
native読込が周波数からλを再構成するための丸め差で、新旧間では完全一致した。
選択/追跡の一致と旧文書との差は別に記録し、共有化の完全一致条件とは混同しない。
同一プロセスで旧/新を交互の順序で測定する。旧指標モジュールは4681e67の自作ソースを保存したもの。
旧経路も現在の公開追跡wrapperを使うが、その数学と再構築は不変で、内部関数呼出しだけが増えている。
旧実行環境全体を再現したベンチマークとは区別する。
独立比較 `out/curved-rf-goal-reuse-verified-20260909` はPASS。
基準は旧コードをnative読込に適用した `out/curved-rf-goal-native-replay-reference-20260909`。
元のsolve直後文書との数値差最大1.550e-13、選択・追跡・その他の文書項目は一致した。
同じ読込済み入力での新旧文書は3形状×各3回、完全一致。
回転楕円体の再solveでも係数/周波数/K/Mは完全一致し、λの差2.274e-13を戻すだけで
元の文書を完全復元できた。新旧比較に数値許容差を導入したわけではない。

| 形状 | 旧中央値[s] | 共有後中央値[s] | 旧/新 |
|---|---:|---:|---:|
| 回転楕円体 | 3.6180 | 1.8942 | 1.9101 |
| 電気対称半球 | 0.7093 | 0.3776 | 1.8785 |
| 磁気対称半球 | 1.0019 | 0.5262 | 1.9042 |

ローカル・同一プロセス・3回交互測定の観測であり、一般的な速度保証ではない。
測定中は標準回帰も別プロセスで実行していた。
旧モジュールのhashは4681e67のgit内容と完全一致する。
初回の未定義変数と、保存直後文書を誤った完全一致基準とした失敗も保持した。
標準回帰 `out/validation-curved-rf-goal-reuse-20260909` はPASS。
677件中675合格・2 skip、405.381秒。seed9モード19量の周波数差0、RF最大相対差8.882e-16。
最終全対象hashと独立比較/標準回帰のhashが一致した。

次の[自動適応の状態遷移](CURVED_RF_ADAPTIVE_PLAN.md)は決定記録であり、まだ実装されていない。
今回の共有は実行内に限定し、その新版でも保存再開の再検証を省略しない。
新規依存・外部資料・旧資産の参照はない。
