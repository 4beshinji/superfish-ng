# 元の平面FEM磁場からの多極係数抽出

2026-09-13 JST。固定833sourceを主839sourceへ統合し、専用API範囲で限定受入。

[受入計画](PLANAR_MAGNETIC_MULTIPOLE_EXTRACTION_PLAN.md)に従い、線形スカラーPlanarMagnetostaticSolutionのP1/P2から係数を抽出する専用APIを追加した。Caseの実FEM再求解で相対/絶対Azと基準の一致を確認してから、元係数からBを評価する。編集済み係数と古い残差を組み合わせても受理しない。閉円板全体について外部境界との距離と交差する全セルを検査し、内部のJz=0と一つの正スカラーmu_rを要求する。円周に現れない内部電流/材料界面も拒否する。

元の片側Bから局所By+iBxを作り、FFTの正方向係数をnormal/skew[T]へ対応させる。主結果は指定N標本からの係数。RでN/2N、.75RでN/2Nの全4系列に座標・セル・元B・係数を保持し、角度差、半径差、負次数成分、打切り残差を別に報告する。Caseと相対AzのSHA256、frameと有限次数を記録する。線形スカラー専用とし、BH/反跳・曲線・軸対称を暗黙に受理しない。

追加5unitは11.648秒でPASS。独立72例は37.935013秒でPASS。双極のP1/P2と四極の厳密P2を48例、四極P1の8/16/32細分を8系列24例で検査した。全て実FEMで求解し、三角形0≤x≤y≤LのAz∝x²−y²、対角線固定Az、水平/鉛直Ht境界を解析対照とした。角度0/.3/−.7、尺度.5/2、振幅1/−7、mu_r/基準Azの変更を含む。製品解への解析係数代入は行わない。

厳密場/係数の最大差6.070e-15、係数座標則8.253e-15は計画1e-10以内。P1の全8系列で係数と元場が細分により改善し、最細の最大差は係数0.008780、元場0.017634で計画0.02以内。固定n=16のN=64/128/256/512系列では角度差が0.002586から0.000432へ減る一方、解析係数差は約0.021〜0.023、元場差は約0.03575。小さい角度差がメッシュ誤差を保証しないことを別々の記録で示している。

全144入力/結果ファイルと833sourceが不変。証拠はout/planar-magnetic-multipole-extraction-development-20260913、独立報告はout/planar-magnetic-multipole-extraction-independent-trial-20260913/report.json、標準はout/validation-planar-magnetic-multipole-extraction-candidate-20260913。合成解析場のみ。前段の多極規約と自前のAz微分/Fourier積分から実装し、追加外部資料・新規依存・旧版実行なし。

保存/CLI、GUIへの受渡し、対象材料の拡張、Maxwell応力/仮想仕事と力/トルクの受入は後続工程。親S05と全計画は未完。

標準2776.678秒、1318合格・任意NGSolve参照2件/HTTP環境1件skip、ResourceWarningなし。主5unitは11.595秒でPASS。標準/独立の固定833sourceと主839source（不変egg-info 6件）は完全一致。独立全例を主で再実行したとは扱わない。旧ベンチマーク・許容差不変。統合証拠は標準出力内seed_regression.json。
