# TEの境界を保つ置換計画自動生成

開始HEAD `cfb2733`、2026-09-15 JST。N04/D02の未接続だったTEの置換計画自動生成を扱う。
受入条件は、元二次境界の頂点/辺/タグの保持、最大1対称面の直接/鏡映、
元内部の番号付けに依存しない生成、独立体積・場/RFの尺度則、CLI/GUI・実Study・保存再生である。

`generate_curved_remesh_plan`のTE一律拒否を、既存のTEケースと最大1対称面の検査へ置き換えた。
TMの直接閉PEC制限、未組立native P2、サイズ・角度・面積・反復・要素予算の条件は保持する。
生成アルゴリズムと固有値/場/RFの計算本体は変更していない。
境界から作り直した計画は既存の`remesh_curved_project`を通り、全履歴段階の二次境界と鏡映幾何を検査する。
生成したplanは元Projectに適用し、元の物理・RF・反射指定を保持する。

最初に8条件（z_min/z_max、電気/磁気対称、直接/鏡映）で従来の拒否を再現した。
新4unitは35.495秒でPASS。全条件の境界/独立円筒体積と鏡映体積、旧内部/番号付けからの独立性、
実Studyの全Eφ/円錐台体積/保存再生、局所細分の固定、2対称面や不正設定の拒否を確認した。
関連`test_curved_remesh_generation`と`test_curved_project_remesh`の17unitは27.894秒でPASS。
旧TE拒否期待を新しい受入へ変更し、旧TMの境界・品質・予算・CLI排他作成の検査は保持した。

## 実円弧とGUI

前段の自己完結した円弧入力を使用し、元55要素から67要素の新しい内部メッシュを生成した。
最大弦辺0.04 m、弦三角形面積0.0003 m²、角度指定1度、最大10000要素/20反復、一様細分1段。
元Caseの角度条件も適用する。CLIとAPIの計画が完全一致した。
記録は`out/te-remesh-generation-20260915/`。

ブラウザー検証器は、固定の楕円パス・履歴段数・パラメーター名・値への依存を、宣言から得る期待値へ変更した。
Project読込完了も固定した履歴行数から判定せず、実際の適用完了を待つ。
RF比較では非該当のnullを明示比較し、NaNの比較式が条件をすり抜けないようにした。
履歴あり/一様段数の固定形状Studyのパラメーターを区別し、TEでは実Eφと元セクターを確認する。

GUIは23項目PASS。計画生成/厳密入力/遅延応答の拒否、2点の実Study、
実Eφの追跡と保存再生、逐次/適応要求の準備、従来固定形状Studyへの切替を確認した。
逐次/適応要求の実行はこの検証の範囲には含めない。
`shape-study.png`を目視し、法則・自動生成欄・2点結果と元セクターの注意書き、R/Q非該当表示を確認した。
外部要求0、実行中の製品336ファイルSHA不変。専用GUIの1ジョブは完了し、
ブラウザー検証器はPASS報告を保存して終了、サーバーPID2812309もSIGINTで終了した。

## 独立物理量と保存証拠

元寸法と2倍寸法を各2点で計算した。初期55要素から生成67要素への切替を含み、
両方とも実Eφ追跡と保存再生がPASS。最小主角overlapは約0.999986594。
元寸法の半領域P2体積は0.0020452922161048274、0.00214116601534768 m³。
直線部の増分は独立式πR²ΔL（R=0.0625 m、ΔL=0.03125×0.25 m）に絶対1e-14 m³以内で一致する。
追跡の全領域体積がこの2倍であることも相対1e-12で確認した。

2倍寸法では接続が一致し、全領域座標2倍、周波数1/2、v=Eφ/r係数2^(-5/2)を確認。
同じエネルギー正規化でU/G不変、Q0は√2倍、壁損失は2^(-3/2)倍。
RF尺度則の相対誤差最大は1.554313e-15。両R/Qは明示的にnullである。
これらは幾何・Maxwell不変量の検査であり、円弧の物理離散化誤差上界ではない。

専用実行はAPI4＋GUI2＝6半領域固有値計算（unit内の実計算は別）。
API元寸法とGUIの2組16 NPZ配列・全RF量が完全一致し、比較対象52 nativeファイルを保持した。
API尺度則側は60保存ファイルを保持。2つの保持数には重複があるため加算しない。
携帯可能な入力と集約結果は
[benchmark](../benchmarks/tuning/te-remesh-generation-20260915.json)、生ログ・場は上記ignored outに保存した。

新しい外部資料・旧コード・依存は使用していない。既存の独立生成とTE検証の入口を接続した変更である。
固有値計算本体・seed経路は変更せず、新/関連unitと専用不変量・GUIの限定検証を採用した。
今回full validatorは再実行していない。直近fullの証拠は
[座標別丸め検証](CURVE_COORDINATE_BOUNDS.md)の1826件（3 skip、HTTP別補完）であり、今回の全件合格とは扱わない。
親33=10受入/16進行/6他未受入/1範囲外は変わらず、D02の元要件照合・他物理とD03等を継続する。
